'use strict';
/** EchoLens tracks - CS Fundamentals free courses (CS-103..CS-107).
 * Companions to CS-101 (C, fc01-c-basics) and CS-102 (C++, fc02-cpp-objects)
 * in tracks/free-micro.js - together the full 7-course curriculum: C, C++,
 * OOP, Python, JavaScript, CSS, HTML. Same shape as every other track file
 * (free: true -> auto-certificate on completion above the pass mark).
 * friendly_grading: true (see store.js's OpenQuest.applyGrade and
 * server.js's /api/open/submit) - these five, plus CS-101/CS-102, skip the
 * standard 10% AI-grading reduction and never surface automated-grading or
 * failure wording to the student; a first-timer's first working program
 * shouldn't come with a visible penalty or a "the grader is broken" scare.
 */

module.exports = [

{
  key: 'cs103-oop', course_code: 'CS-103', free: true, friendly_grading: true, default_language: 'cpp',
  title: 'CS-103: Fundamentals of Object-Oriented Programming (OOP)',
  description: 'Architectural paradigm centered around encapsulation, abstraction, class inheritance, and dynamic polymorphism, taught in C++. Ten topics, three tasks each, certificate at the end.',
  outcome: 'Enforce structural data encapsulation and access control boundaries within object designs; reuse and extend class definitions using single/multiple inheritance; apply dynamic runtime polymorphism via method overriding, virtual functions, and abstract interfaces.',
  keywords: ['object oriented programming basics', 'OOP fundamentals', 'free OOP course', 'learn OOP in C++', 'classes and objects for beginners'],
  key_concepts: ['Classes and objects', 'Encapsulation', 'Constructors and destructors', 'Overloading vs overriding', 'Inheritance', 'Virtual functions', 'Abstract classes', 'Static members', 'Interfaces', 'Composition vs aggregation'],
  pass_mark: 60, titleNames: ['Object Rookie', 'Design Architect'],
  levels: [
    { no: 1, week: 1, session: 1, title: 'Classes & Objects', video_url: 'https://www.youtube.com/watch?v=h1NlXUaDpmk', topic: 'Classes act as user-defined blueprints containing fields and functions, while objects are active instances.\n\n#include <iostream>\nusing namespace std;\nclass Car {\npublic:\n    string brand;\n    void drive() { cout << "Driving..." << endl; }\n};\nint main() { Car c; c.drive(); return 0; }',
      problems: [
        { title: 'Car Class', points: 30, difficulty: 'Basic',
          description: "A class is a blueprint that groups related data and behavior under one name, while an object is a concrete instance created from that blueprint. Write a class named Car with a public brand field (a string), so the class compiles cleanly with that field accessible.",
          criteria: ['brand is declared under a public: label', 'The class compiles cleanly'],
          hint: 'Declare it under the public: label.',
          solution: 'class Car { public: string brand; };' },
        { title: 'Object Instantiation', points: 30, difficulty: 'Basic',
          description: "Every object created from a class gets its own independent copy of its fields, so changing one object's data never affects another. Write a program that instantiates two separate Car objects and gives each a different brand value, showing they don't interfere with each other.",
          criteria: ['Two separate Car variables exist', 'Each has its own independent brand value'],
          hint: 'Car car1; car1.brand = "Toyota";.',
          solution: 'Two Car objects, each set to a different brand.' },
        { title: 'Class Method', points: 40, difficulty: 'Core',
          description: "A class can bundle behavior alongside its data by defining methods directly inside the class body. Write a drive() method inside the Car class that prints a message when called, and invoke it from main() using an object, e.g. c.drive();.",
          criteria: ['drive() is defined inside the class body', 'Calling it from main prints the message'],
          hint: 'Define the method inside the class body.',
          solution: 'drive() prints a message, called as c.drive();.' },
      ] },
    { no: 2, week: 1, session: 2, title: 'Encapsulation', video_url: 'https://www.youtube.com/watch?v=GqqujGu06ag', topic: 'Encapsulation protects raw state by making fields private and granting controlled access through public getters/setters.\n\n#include <iostream>\nusing namespace std;\nclass Bank {\n    double balance;\npublic:\n    void setBal(double b) { if(b>=0) balance = b; }\n};\nint main() { Bank b; b.setBal(100); return 0; }',
      problems: [
        { title: 'Private Member', points: 30, difficulty: 'Basic',
          description: "Encapsulation hides an object's internal state so outside code can't touch it directly - in C++, members are private by default before a public: label. Write a class with a private double balance member, and confirm that trying to access it directly from outside the class fails to compile.",
          criteria: ['balance is declared private (the default before a public: label)', 'Direct outside access to it fails to compile'],
          hint: 'Direct variable access from outside the class should fail to compile.',
          solution: 'private double balance; with no public access to the raw field.' },
        { title: 'Setter Validation', points: 40, difficulty: 'Core',
          description: "A setter method is the controlled doorway into a private field, and it can reject bad values before they're ever stored. Write a setter method for a balance field that only assigns the new value if it is 0 or greater, leaving balance unchanged when a negative number is passed in.",
          criteria: ['The assignment is wrapped in a validation check', 'A negative input leaves balance unchanged'],
          hint: 'Wrap the value assignment in an if condition.',
          solution: 'if (b >= 0) balance = b; - negative values are silently rejected.' },
        { title: 'Getter Method', points: 40, difficulty: 'Core',
          description: "A getter method is the read-only counterpart to a setter, handing back a private field's value without exposing the field itself. Write a public getter method that simply returns the exact value currently stored in a private member.",
          criteria: ['A public method returns the private variable', 'The returned value matches exactly what was set'],
          hint: 'The method simply returns the private variable.',
          solution: 'double getBal() { return balance; }' },
      ] },
    { no: 3, week: 1, session: 3, title: 'Constructors & Destructors', video_url: 'https://www.youtube.com/watch?v=hRVfMTuFJNE', topic: 'Constructors initialize object state upon instantiation, while destructors execute cleanup tasks upon object destruction.\n\n#include <iostream>\nusing namespace std;\nclass Item {\npublic:\n    Item() { cout << "Created" << endl; }\n    ~Item() { cout << "Destroyed" << endl; }\n};\nint main() { Item i; return 0; }',
      problems: [
        { title: 'Default Constructor', points: 30, difficulty: 'Basic',
          description: "A constructor runs automatically the moment an object is created, and it shares the class's exact name with no return type. Write a constructor that initializes a name member to 'Unknown' as soon as an object is created, so a freshly-made object already has that default value.",
          criteria: ['The constructor matches the class name, no return type', "name is 'Unknown' immediately after object creation"],
          hint: 'Matches the class name, without a return type.',
          solution: 'ClassName() { name = "Unknown"; }' },
        { title: 'Parameterized Constructor', points: 40, difficulty: 'Core',
          description: "A constructor can also accept parameters, letting the caller supply starting values for an object's members right at creation time. Write a constructor that takes at least one parameter and uses it to set a member's initial value, either through direct assignment or a member initializer list.",
          criteria: ['The constructor takes at least one parameter', 'Members are set from the passed-in arguments'],
          hint: 'Use assignment or a member initializer list.',
          solution: 'ClassName(type param) initializing a member from param.' },
        { title: 'Destructor Message', points: 40, difficulty: 'Core',
          description: "A destructor runs automatically when an object's lifetime ends, marked with a tilde before the class name, and is the natural place to print or perform cleanup. Write a destructor (~ClassName()) that prints a cleanup message, and confirm it fires when the object goes out of scope.",
          criteria: ['The destructor uses the tilde-prefix syntax', 'The message prints when the object goes out of scope'],
          hint: 'Tilde prefix syntax: ~ClassName().',
          solution: '~ClassName() { cout << "cleanup message"; }' },
      ] },
    { no: 4, week: 2, session: 1, title: 'Overloading vs Overriding', video_url: 'https://www.youtube.com/watch?v=nrR7smJpguY', topic: 'Method overloading varies parameters at compile-time; method overriding redefines parent class methods in child classes.\n\n#include <iostream>\nusing namespace std;\nclass Animal {\npublic:\n    void sound() { cout << "Generic" << endl; }\n};\nclass Dog : public Animal {\npublic:\n    void sound() { cout << "Bark" << endl; }\n};',
      problems: [
        { title: 'Compile-Time Polymorphism', points: 30, difficulty: 'Basic',
          description: "Overloading lets multiple functions share one name as long as their parameter types differ, with the compiler picking the right version at compile time based on the argument type. Write two functions both named print() - one taking an int, one taking a string - and call each with a matching argument to confirm the compiler picks the correct overload.",
          criteria: ['Both functions share the name print', 'Each is invoked correctly for its matching type'],
          hint: 'Same name, different parameter types.',
          solution: 'print(int) and print(string) overloads, each called once.' },
        { title: 'Base Class Method', points: 30, difficulty: 'Basic',
          description: "Before you can override anything, you need a base class defining the method that a derived class will later redefine. Write a base class Animal with a sound() method that prints something generic, and confirm calling it on an Animal object works and compiles.",
          criteria: ['Animal has a sound() method', 'Calling it on an Animal object works and compiles'],
          hint: 'A basic method declaration in the base class.',
          solution: 'class Animal { public: void sound() { ... } };' },
        { title: 'Derived Class Override', points: 40, difficulty: 'Core',
          description: "Overriding redefines a parent class's method in a child class, using the exact same method signature so the child's version replaces the parent's when called on a child object. Write a Dog class that inherits from Animal (Dog : public Animal) and redefines sound() with an identical signature, then confirm a Dog object prints the Dog version, not the Animal one.",
          criteria: ['Dog : public Animal redeclares sound() with the exact same signature', 'A Dog object prints the Dog version, not the Animal version'],
          hint: 'Match the exact method signature in the child class.',
          solution: 'Dog::sound() overrides Animal::sound(), verified by calling it on a Dog.' },
      ] },
    { no: 5, week: 2, session: 2, title: 'Inheritance Basics', video_url: 'https://www.youtube.com/watch?v=rJlJ8qqVm3k', topic: 'Inheritance allows a child class to acquire attributes and behaviors from a base parent class cleanly.\n\n#include <iostream>\nusing namespace std;\nclass Parent { public: void speak() { cout << "Hi"; } };\nclass Child : public Parent {};\nint main() { Child c; c.speak(); return 0; }',
      problems: [
        { title: 'Single Inheritance', points: 30, difficulty: 'Basic',
          description: "Inheritance lets one class acquire the fields and methods of another without rewriting them, using the class Child : public Parent {}; syntax. Write a Child class that inherits publicly from a Parent class, and confirm the program compiles with the inheritance relationship in place.",
          criteria: ['class Child : public Parent {}; syntax is used', 'The program compiles with the inheritance in place'],
          hint: 'class Dog : public Animal {};.',
          solution: 'A child class publicly inheriting from a parent.' },
        { title: 'Inherited Method Call', points: 30, difficulty: 'Basic',
          description: "A child class automatically has access to every method its parent defines, even ones the child never redeclares itself. Write a program where a child object calls a method that only exists on the parent class (no override), confirming the parent's version runs.",
          criteria: ['No override of the method exists on the child', "Calling it on the child object runs the parent's version"],
          hint: 'Call it directly: myDog.eat();.',
          solution: 'A child object calling an unoverridden parent method.' },
        { title: 'Protected Access', points: 40, difficulty: 'Core',
          description: "The protected specifier sits between private and public - it's hidden from outside code but still directly accessible to derived classes, unlike a fully private member. Write a class with a member declared protected, and a derived class whose method reads or writes that member directly.",
          criteria: ['The member is under a protected: label, not private', 'A derived class method can read/write it directly'],
          hint: 'Place the variable under a protected: label.',
          solution: 'protected member accessed successfully from a derived class method.' },
      ] },
    { no: 6, week: 2, session: 3, title: 'Virtual Functions', video_url: 'https://www.youtube.com/watch?v=77eueMbWI0Y', topic: 'Virtual functions enable runtime dynamic polymorphism, invoking child class overrides via parent references.\n\n#include <iostream>\nusing namespace std;\nclass Base { public: virtual void show() { cout << "Base"; } };\nclass Derived : public Base { public: void show() override { cout << "Derived"; } };\nint main() { Base *b = new Derived(); b->show(); return 0; }',
      problems: [
        { title: 'Virtual Method', points: 30, difficulty: 'Basic',
          description: "Marking a method virtual in the base class is what allows a derived class's version to be chosen at runtime instead of compile time. Write a base class method marked virtual by placing the virtual keyword before its return type, confirming it still compiles and runs normally.",
          criteria: ['The virtual keyword precedes the return type', 'The method still compiles and runs normally'],
          hint: 'Add the virtual keyword before the return type.',
          solution: 'virtual void show() { ... } in the base class.' },
        { title: 'Polymorphic Call', points: 30, difficulty: 'Basic',
          description: "A base class pointer can point at a derived class object without slicing, as long as the base method involved is virtual - this is the setup that makes runtime polymorphism possible. Write a program that assigns a heap-allocated Derived object to a Base* pointer (e.g. Base *b = new Derived();), and confirm it compiles and runs without errors.",
          criteria: ['A Base* points at a new Derived()', 'The program compiles and runs without slicing errors'],
          hint: 'Animal* a = new Dog();.',
          solution: 'Base* pointing to a heap-allocated Derived object.' },
        { title: 'Runtime Invocation', points: 40, difficulty: 'Core',
          description: "Calling a virtual method through a base class pointer dispatches to the derived class's override at runtime, not the base class's original version - this is dynamic dispatch in action. Write a program that calls a virtual method through a base class pointer pointing at a derived object, and confirm the output shows the derived class's version ran, not the base class's.",
          criteria: ['The base method is declared virtual', 'Calling through the base pointer prints the Derived output, not Base'],
          hint: 'This executes the derived code via the virtual table.',
          solution: 'Base* calling show() prints "Derived", proving dynamic dispatch.' },
      ] },
    { no: 7, week: 3, session: 1, title: 'Abstract Classes', video_url: 'https://www.youtube.com/watch?v=XNHSSduMBbY', topic: 'Abstract classes contain at least one pure virtual function (= 0) and cannot be instantiated directly.\n\n#include <iostream>\nusing namespace std;\nclass Shape { public: virtual void draw() = 0; };\nclass Circle : public Shape { public: void draw() override { cout << "Circle"; } };\nint main() { Circle c; c.draw(); return 0; }',
      problems: [
        { title: 'Pure Virtual Method', points: 30, difficulty: 'Basic',
          description: "A pure virtual function has no implementation in its class at all, marked with = 0;, and any class containing one automatically becomes abstract. Write a class with a pure virtual function declared as virtual void draw() = 0;, and confirm the containing class is now abstract.",
          criteria: ['The syntax virtual void draw() = 0; is used', 'The containing class is now abstract'],
          hint: 'Syntax: virtual void draw() = 0;.',
          solution: 'An abstract base class with one pure virtual method.' },
        { title: 'Concrete Class', points: 40, difficulty: 'Core',
          description: "A derived class only becomes usable (concrete) once it provides real implementations for every pure virtual method it inherited. Write a class that derives from an abstract base and implements its pure virtual method fully, then confirm the derived class can actually be instantiated.",
          criteria: ['All pure virtual methods are implemented', 'The derived class can be instantiated'],
          hint: 'Implement every pure virtual method from the base.',
          solution: 'Circle : public Shape implementing draw() fully.' },
        { title: 'Prevent Instantiation', points: 40, difficulty: 'Core',
          description: "An abstract class can never be instantiated on its own - the compiler enforces this, since an object of that type could never provide the missing pure virtual implementation. Write a short demonstration attempting to create an object of an abstract class directly, and explain why that attempt fails to compile.",
          criteria: ['An attempt to create a Shape object directly is shown', 'The explanation states abstract classes cannot exist as objects'],
          hint: 'Abstract classes cannot exist as objects.',
          solution: 'Shape s; is identified as a compile error, with the reason stated.' },
      ] },
    { no: 8, week: 3, session: 2, title: 'Static Members', video_url: 'https://www.youtube.com/watch?v=7u7ti-S69uY', topic: 'Static members belong to the class type itself rather than individual instances, sharing data across objects.\n\n#include <iostream>\nusing namespace std;\nclass Counter {\npublic:\n    static int count;\n    Counter() { count++; }\n};\nint Counter::count = 0;\nint main() { Counter c1, c2; cout << Counter::count; return 0; }',
      problems: [
        { title: 'Static Counter', points: 30, difficulty: 'Basic',
          description: "A static member belongs to the class itself rather than to any single object, so every instance shares and updates the exact same copy. Write a class with a static int count that increments once inside the constructor, so it tracks the total number of instances created so far.",
          criteria: ['count is declared static', 'It increments once per constructor call'],
          hint: 'Increment the static count inside the constructor.',
          solution: 'static int count; incremented in the constructor.' },
        { title: 'Static Initializer', points: 30, difficulty: 'Basic',
          description: "A static member must be defined once outside the class, at file scope, using the ClassName::member = value; syntax, separately from where it was declared inside the class. Write the out-of-class initializer for a static member, e.g. int Counter::count = 0;, appearing exactly once outside any function.",
          criteria: ['The initializer uses the ClassName::member = value; syntax', 'It appears once, outside any function'],
          hint: 'int Counter::count = 0;.',
          solution: 'int ClassName::count = 0; at file scope.' },
        { title: 'Static Function', points: 40, difficulty: 'Core',
          description: "A static method can be called on the class itself rather than on any particular object, using the scope resolution operator (ClassName::method()). Write a static method that returns the current value of a static counter, and call it using scope resolution, e.g. Counter::getCount().",
          criteria: ['The method is declared static', 'It is called using scope resolution, e.g. Counter::getCount()'],
          hint: 'Call using scope resolution: Counter::getCount().',
          solution: 'static int getCount() { return count; } called via ClassName::getCount().' },
      ] },
    { no: 9, week: 3, session: 3, title: 'Interfaces', video_url: 'https://www.youtube.com/watch?v=BkrISFxSn-4', topic: 'Interfaces define pure contract specifications containing pure virtual functions without any variable state.\n\n#include <iostream>\nusing namespace std;\nclass IPrintable { public: virtual void print() = 0; };\nclass Doc : public IPrintable { public: void print() override { cout << "Doc"; } };\nint main() { Doc d; d.print(); return 0; }',
      problems: [
        { title: 'Interface Design', points: 30, difficulty: 'Basic',
          description: "An interface in C++ is really just a class made entirely of pure virtual methods with no data of its own, describing a contract rather than an implementation. Write a class containing only pure virtual method declarations (= 0;) and no data fields at all.",
          criteria: ['No data fields exist on the class', 'Every method is pure virtual (= 0;)'],
          hint: 'No data fields allowed - only = 0; methods.',
          solution: 'A class containing only pure virtual method declarations.' },
        { title: 'Interface Implementation', points: 40, difficulty: 'Core',
          description: "Implementing an interface means inheriting from it publicly and providing a real body for every one of its pure virtual methods. Write a concrete class that inherits publicly from an interface and implements every one of its methods.",
          criteria: ['The class inherits publicly from the interface', 'Every interface method is implemented'],
          hint: 'Inherit publicly and implement the methods.',
          solution: 'Doc : public IPrintable implementing print() fully.' },
        { title: 'Multiple Interfaces', points: 40, difficulty: 'Core',
          description: "C++ allows a class to inherit from more than one interface at once, fulfilling multiple separate contracts in a single class. Write a class that inherits from two different interfaces (e.g. class File : public IRead, public IWrite) and implements the methods required by both.",
          criteria: ['The class lists both interfaces in its inheritance list', 'Methods from both interfaces are implemented'],
          hint: 'class File : public IRead, public IWrite.',
          solution: 'One class implementing two separate interfaces fully.' },
      ] },
    { no: 10, week: 4, session: 1, title: 'Composition vs Aggregation', video_url: 'https://www.youtube.com/watch?v=B46RqPYhEys', topic: 'Composition reflects tight ownership where child objects die with the parent; aggregation reflects loose independent association.\n\n#include <iostream>\nusing namespace std;\nclass Engine {};\nclass Car { Engine e; }; // Composition\nint main() { Car c; return 0; }',
      problems: [
        { title: 'Composition', points: 30, difficulty: 'Basic',
          description: "Composition models tight ownership: a child object lives directly inside the parent as a member, so it's created and destroyed along with the parent automatically. Write a class that embeds another object directly as a member (e.g. class Car { Engine e; };), and confirm destructing the parent frees the child automatically.",
          criteria: ['The child object is a direct (non-pointer) member', 'Destructing the parent frees the child automatically'],
          hint: 'Destructing the parent frees the child.',
          solution: 'class Car { Engine e; }; - Engine lives and dies with Car.' },
        { title: 'Aggregation', points: 40, difficulty: 'Core',
          description: "Aggregation models a looser relationship where the parent merely references another object through a pointer, without owning or managing its lifecycle. Write a class that stores a pointer to another object (e.g. class Team { Player* p; };) without creating or destroying that object itself.",
          criteria: ['The parent stores a pointer, not an owned instance', 'The parent does not manage the pointed-to object\'s lifecycle'],
          hint: 'Store the pointer without managing its lifecycle.',
          solution: 'class Team { Player* p; }; where Player is created/destroyed independently.' },
        { title: 'Lifecycle Verification', points: 60, difficulty: 'Boss',
          description: "Because aggregation doesn't tie a child's lifetime to its parent's, a child object created independently should keep working even after the aggregating parent is destroyed. Write a program where a child object is created outside its aggregating parent in main(), the parent is destroyed first, and the child object remains valid and usable afterward.",
          criteria: ['The child instance is created outside the parent', 'The child remains valid/usable after the parent is destroyed'],
          hint: 'Create child instances outside the parent.',
          solution: 'A Player created in main, referenced by a Team that is destroyed first - Player still works after.' },
      ] },
  ],
},

{
  key: 'cs104-python', course_code: 'CS-104', free: true, friendly_grading: true, default_language: 'python',
  title: 'CS-104: Fundamentals of Python',
  description: 'High-level dynamically typed programming, rich built-in data structures, list comprehensions, and functional module composition. Ten topics, three tasks each, certificate at the end.',
  outcome: 'Write clean, standard Python scripts utilizing dynamic typing, dynamic structures, and explicit indentation; manipulate high-level sequence and mapping structures (list, tuple, dict, set); build modular scripts utilizing list comprehensions, built-in functions, and custom modules.',
  keywords: ['fundamentals of Python', 'basics of Python programming', 'free Python course', 'learn Python online', 'Python for beginners'],
  key_concepts: ['Syntax and print', 'Variables and dynamic types', 'User input and conversions', 'Control flow', 'Loops', 'Lists and tuples', 'Dictionaries and sets', 'Functions and scope', 'List comprehensions', 'Modules and imports'],
  pass_mark: 60, titleNames: ['Python Starter', 'Pythonista'],
  levels: [
    { no: 1, week: 1, session: 1, title: 'Python Syntax & Print', video_url: 'https://www.youtube.com/watch?v=rfscVS0vtbw', topic: 'Python uses clean block indentation instead of curly braces to define scope boundaries.\n\nprint("Welcome to Python!")\nx = 10\nif x > 5:\n    print("x is greater than 5")',
      problems: [
        { title: 'Hello Python', points: 30, difficulty: 'Basic',
          description: "Python programs commonly start with the print() function, the simplest way to display text to the screen. Write a program that outputs the exact greeting 'Hello, World!' using a single print() call.",
          criteria: ['print() is used', "Output is exactly: Hello, World!"],
          hint: 'print("Hello, World!").',
          solution: 'One print() call with the exact greeting.' },
        { title: 'Dynamic Assignment', points: 30, difficulty: 'Basic',
          description: "Unlike many other languages, Python never requires you to declare a variable's type - the type is inferred automatically from whatever value you assign. Write a program that assigns at least an int, a float, and a string to three different variables, without using any type keyword anywhere.",
          criteria: ['At least an int, a float, and a string variable are assigned', 'No type keyword is used anywhere'],
          hint: 'x = 5; y = 3.14; z = "Text".',
          solution: 'Three variables of different types, assigned with no type annotation.' },
        { title: 'Multi-line Strings', points: 40, difficulty: 'Core',
          description: "Triple quotes (\"\"\" ... \"\"\") let a string span several lines exactly as written, which is useful for printing a whole paragraph at once. Write a program that prints a multi-line paragraph using triple-quoted strings, preserving the line breaks in the output.",
          criteria: ['Triple quotes """ ... """ wrap the text', 'The output spans multiple lines'],
          hint: 'Use triple quotes """ ... """.',
          solution: 'A triple-quoted string printed, preserving line breaks.' },
      ] },
    { no: 2, week: 1, session: 2, title: 'Variables & Dynamic Types', video_url: 'https://www.youtube.com/watch?v=eBReeuBz7n4', topic: 'Python infers variable types automatically at runtime without explicit type annotations.\n\ncount = 10\nprice = 19.99\nname = "EchoLens"\nprint(f"{name} costs ${price}")',
      problems: [
        { title: 'Type Checking', points: 30, difficulty: 'Basic',
          description: "Because Python infers types automatically, the built-in type() function is how you can inspect what type a value actually turned out to be at runtime. Write a program that prints the type of a value using type(), and confirm the printed type matches what you'd expect for that value.",
          criteria: ['type() is called on the value', 'The printed type matches the actual value'],
          hint: 'print(type(val)).',
          solution: 'type(val) printed directly.' },
        { title: 'Type Casting', points: 30, difficulty: 'Basic',
          description: "Values read as text sometimes need to be converted into numbers before you can do arithmetic with them, using int() or float() to cast explicitly. Write a program that casts the string '100' into an integer using int(), then uses the result in a calculation to prove it now behaves as a real number.",
          criteria: ['int() performs the cast', 'The result behaves as a number afterward (e.g. can be added to)'],
          hint: 'int("100").',
          solution: 'int("100") stored and used as a real integer.' },
        { title: 'F-String Formatting', points: 40, difficulty: 'Core',
          description: "An f-string lets you embed variables directly inside a string by prefixing it with f and wrapping the variable name in curly braces, avoiding messy string concatenation. Write a program that formats some text using an f-string, interpolating at least one variable inside {}.",
          criteria: ['The string is prefixed with f', 'At least one variable is interpolated inside {}'],
          hint: 'Prefix the string with the letter f.',
          solution: 'f"..." with a variable embedded in braces.' },
      ] },
    { no: 3, week: 1, session: 3, title: 'User Input & Conversions', video_url: 'https://www.youtube.com/watch?v=hXOGilZyg6Y', topic: 'The input() function reads keyboard input as strings, requiring explicit numeric type casting.\n\nage = int(input("Enter age: "))\nprint(f"Next year: {age + 1}")',
      problems: [
        { title: 'String Input', points: 30, difficulty: 'Basic',
          description: "The input() function always returns whatever the user types as a plain string, with no automatic conversion. Write a program that reads the user's name using input() and prints it back exactly as typed, without any numeric casting.",
          criteria: ['input() is called with no forced numeric cast', 'The typed name is printed back'],
          hint: 'name = input().',
          solution: 'input() stored as a string and printed.' },
        { title: 'Number Input', points: 30, difficulty: 'Basic',
          description: "Since input() always returns a string, reading a number requires wrapping the call in float() (or int()) to convert it before doing arithmetic. Write a program that reads two numbers using float(input()) and prints their sum.",
          criteria: ['Both values pass through float(input())', 'The printed sum is numerically correct'],
          hint: 'a = float(input()).',
          solution: 'Two float(input()) reads, summed and printed.' },
        { title: 'Age Calculator', points: 40, difficulty: 'Core',
          description: "Combining input(), a numeric cast, and simple arithmetic is the standard pattern for turning a piece of user-entered data into a computed result. Write a program that reads a birth year as an integer using int(input()), then computes and prints the person's age by subtracting that year from 2026.",
          criteria: ['The birth year is read and cast to int', 'The computed age is 2026 minus that year'],
          hint: '2026 - int(input_year).',
          solution: 'int(input()) subtracted from 2026 and printed.' },
      ] },
    { no: 4, week: 2, session: 1, title: 'Control Flow (if/elif/else)', video_url: 'https://www.youtube.com/watch?v=Zp5MuPOtsSY', topic: 'Branch execution paths cleanly using human-readable if, elif, and else statements.\n\nscore = 85\nif score >= 90:\n    print("A")\nelif score >= 50:\n    print("Pass")\nelse:\n    print("Fail")',
      problems: [
        { title: 'Check Positivity', points: 30, difficulty: 'Basic',
          description: "An if/elif/else chain lets a program branch across more than two possible outcomes, checking each condition in order until one matches. Write a program that reads a number and prints 'Positive', 'Negative', or 'Zero' depending on its value, using an if/elif/else sequence that correctly covers all three cases.",
          criteria: ['An if/elif/else sequence covers all three cases', 'Correct for a positive, a negative, and zero'],
          hint: 'Use an if, elif, and else sequence.',
          solution: 'Three-branch conditional handling every case correctly.' },
        { title: 'Logical Operators', points: 30, difficulty: 'Basic',
          description: "The and operator lets you combine two conditions into one check that only passes when both are true - useful for testing divisibility by more than one number at once. Write a program that reads a number and checks whether it is divisible by both 3 and 5 using a single if statement joined with and.",
          criteria: ['Both conditions are joined with and', 'Correct for a number that passes both, and one that fails one'],
          hint: 'if num % 3 == 0 and num % 5 == 0:.',
          solution: 'A single if using and across both modulo checks.' },
        { title: 'Ternary Assignment', points: 40, difficulty: 'Core',
          description: "Python's conditional expression (X if condition else Y) lets you assign one of two values to a variable in a single line, instead of writing a full if/else block. Write a program that assigns a status string inline based on an age value, e.g. status = 'Adult' if age >= 18 else 'Minor', and prints the result for both cases.",
          criteria: ['The assignment uses the X if condition else Y form', 'Correct for both branches of the condition'],
          hint: 'status = "Adult" if age >= 18 else "Minor".',
          solution: 'A single-line ternary assignment, correct both ways.' },
      ] },
    { no: 5, week: 2, session: 2, title: 'Loops (for/while)', video_url: 'https://www.youtube.com/watch?v=6iF8Xb7Z3wQ', topic: 'Iterate over sequences using for loops or repeat code conditionally using while loops.\n\nfor i in range(1, 4):\n    print(i)\n\ncount = 3\nwhile count > 0:\n    count -= 1',
      problems: [
        { title: 'Range Loop', points: 30, difficulty: 'Basic',
          description: "range() can take a step argument, letting a for loop skip through values instead of visiting every single number one by one. Write a program that prints every even number from 2 to 20 using range() with a step of 2, rather than looping through every number and filtering.",
          criteria: ['range() with a step is used, not a manual filter', 'Output is 2, 4, 6 ... 20'],
          hint: 'range(2, 21, 2).',
          solution: 'for i in range(2, 21, 2): print(i)' },
        { title: 'While Decrement', points: 30, difficulty: 'Basic',
          description: "A while loop repeats as long as its condition stays true, which is a natural fit for counting down toward zero. Write a program that counts down from 10 to 1 using a while loop that decrements a counter each pass, printing every number along the way.",
          criteria: ['A while loop drives the countdown', 'All ten numbers appear in descending order'],
          hint: 'count -= 1 inside the loop.',
          solution: 'while count > 0: print then count -= 1.' },
        { title: 'Loop Control', points: 40, difficulty: 'Core',
          description: "The break statement immediately exits a loop the moment a condition is met, skipping any remaining iterations. Write a program that loops through a sequence of numbers and uses break to stop as soon as the value 5 is encountered, so nothing after 5 gets printed.",
          criteria: ['The break statement is used', 'Nothing after 5 is printed'],
          hint: 'Execute the statement break.',
          solution: 'if x == 5: break inside the loop body.' },
      ] },
    { no: 6, week: 2, session: 3, title: 'Lists & Tuples', video_url: 'https://www.youtube.com/watch?v=W8KRzm-HUcc', topic: 'Lists [] are ordered mutable sequences; tuples () are permanent immutable sequences.\n\nmy_list = [10, 20, 30]\nmy_list.append(40)\nmy_tuple = (1, 2, 3)',
      problems: [
        { title: 'List Operations', points: 30, difficulty: 'Basic',
          description: "Lists are mutable, so you can grow them with .append() and remove elements from a specific position with .pop(index). Write a program that appends a new item to a list using .append(), then removes the first element using .pop(0), and prints the final list.",
          criteria: ['.append() adds the new item', '.pop(0) removes the first element'],
          hint: 'Use .append() and .pop(0).',
          solution: 'append() then pop(0), final list printed.' },
        { title: 'List Slicing', points: 30, difficulty: 'Basic',
          description: "Slicing lets you pull out a sub-section of a list using the my_list[start:stop] syntax, without writing a manual loop. Write a program that extracts exactly the first 3 elements of a list using a slice expression, and prints the result.",
          criteria: ['A slice expression is used, not a manual loop', 'Exactly 3 elements are returned'],
          hint: 'Slice construct: my_list[0:3].',
          solution: 'my_list[0:3] printed.' },
        { title: 'Tuple Unpacking', points: 40, difficulty: 'Core',
          description: "Unpacking lets you assign every element of a tuple to its own variable in a single line, matching them up by position. Write a program that unpacks a 3-item tuple into three separate variables in one line (e.g. x, y, z = point), and prints each one to confirm it holds the correct value.",
          criteria: ['Unpacking assigns all three in one line', 'Each variable holds the correct corresponding value'],
          hint: 'x, y, z = point.',
          solution: 'x, y, z = point then all three printed correctly.' },
      ] },
    { no: 7, week: 3, session: 1, title: 'Dictionaries & Sets', video_url: 'https://www.youtube.com/watch?v=daefaLgNkw0', topic: 'Dictionaries map key-value pairs; sets store unordered collections of unique elements.\n\nuser = {"name": "Alice", "age": 25}\nprint(user.get("name"))\nunique_nums = {1, 2, 2, 3} # {1, 2, 3}',
      problems: [
        { title: 'Dict Access', points: 30, difficulty: 'Basic',
          description: ".get() looks up a key in a dictionary and, unlike bracket indexing, won't crash the program if the key happens to be missing. Write a program that retrieves a value from a dictionary using .get() (not bracket indexing), and prints the correct value for a known key.",
          criteria: ['.get() is used, not bracket indexing', 'The correct value for the key is returned'],
          hint: 'grades.get("Math").',
          solution: '.get("key") returning the matching value.' },
        { title: 'Dict Iteration', points: 30, difficulty: 'Basic',
          description: "The .items() method lets a for loop pull out both the key and the value of every entry in a dictionary at once. Write a program that loops over a dictionary's entries using for key, val in d.items():, printing both the key and value for every entry.",
          criteria: ['.items() is used in the for loop', 'Both the key and value are printed for every entry'],
          hint: 'for key, val in student.items():.',
          solution: 'for key, val in d.items(): printing both.' },
        { title: 'Deduplicate List', points: 40, difficulty: 'Core',
          description: "A set automatically discards duplicate values, which makes set() a quick way to deduplicate a list without writing your own comparison logic. Write a program that converts a list containing duplicate values into a set using set(), and prints the result to confirm every duplicate is gone.",
          criteria: ['set() removes the duplicates', 'The resulting collection has no repeated values'],
          hint: 'unique_set = set(duplicate_list).',
          solution: 'set(list_with_dupes) printed with duplicates gone.' },
      ] },
    { no: 8, week: 3, session: 2, title: 'Functions & Scope', video_url: 'https://www.youtube.com/watch?v=9Os0o3wzS_I', topic: 'Define reusable functions using def, handle default arguments, and manage scopes.\n\ndef greet(name="User"):\n    return f"Hello {name}"\n\nprint(greet("Bob"))',
      problems: [
        { title: 'Simple Function', points: 30, difficulty: 'Basic',
          description: "A default parameter value lets a function be called with fewer arguments than it defines, falling back to a preset value for any that are left out. Write a function that returns the product of its two arguments, giving the second parameter a default value, and call it both with and without supplying that second argument.",
          criteria: ['At least one parameter has a default value', 'Calling with and without the argument both work'],
          hint: 'Define a default parameter value.',
          solution: 'def multiply(a, b=2): return a*b, called both ways.' },
        { title: 'Variable Scope', points: 40, difficulty: 'Core',
          description: "By default, assigning to a variable inside a function creates a new local variable - reaching outward to change a global variable requires the global keyword first. Write a function that modifies a global variable by declaring global my_var inside it, then confirm the change is visible outside the function afterward.",
          criteria: ['global my_var is declared inside the function', 'The change is visible outside the function afterward'],
          hint: 'Declare global my_var inside the function.',
          solution: 'global my_var then reassigned; the outer value reflects the change.' },
        { title: 'Arbitrary Args', points: 40, difficulty: 'Core',
          description: "The *args syntax lets a function accept any number of positional arguments, collecting them into a tuple you can loop over inside the function body. Write a function that accepts *args, loops over every value it receives, and does something with each one (such as summing or printing them).",
          criteria: ['The parameter uses the *args form', 'The function loops over all elements in args'],
          hint: 'Loop over the elements in the args tuple.',
          solution: 'def total(*args): loops over args summing/printing them.' },
      ] },
    { no: 9, week: 3, session: 3, title: 'List Comprehensions', video_url: 'https://www.youtube.com/watch?v=E1ZAVEmRwyI', topic: 'List comprehensions map and filter existing sequences into new lists in a single line.\n\nnums = [1, 2, 3, 4, 5]\nsquares = [x**2 for x in nums if x % 2 == 0]\nprint(squares) # [4, 16]',
      problems: [
        { title: 'Square Numbers', points: 30, difficulty: 'Basic',
          description: "A list comprehension builds a new list from an existing sequence in a single expression, combining a loop and a transformation without writing a separate loop block. Write a program that builds a list of the squares of the numbers 1 through 10 using a single comprehension expression, e.g. [x**2 for x in range(1, 11)].",
          criteria: ['A single comprehension expression produces the list', 'All ten squares are present in order'],
          hint: '[x**2 for x in range(1, 11)].',
          solution: 'One-line comprehension producing 10 squares.' },
        { title: 'Filter Evens', points: 30, difficulty: 'Basic',
          description: "Adding an if clause to the end of a comprehension filters which elements make it into the resulting list, based on a condition you choose. Write a program that filters the odd numbers out of a list using a comprehension with an if condition (not a separate loop), leaving only the even numbers.",
          criteria: ['The comprehension has an if condition, not a separate loop', 'Only even numbers remain'],
          hint: 'Append if x % 2 == 0 at the end.',
          solution: '[x for x in nums if x % 2 == 0]' },
        { title: 'String Uppercase', points: 40, difficulty: 'Core',
          description: "A comprehension can apply a transformation - like calling a string method - to every element of a list as it builds the new one. Write a program that converts every word in a list to uppercase using a comprehension that calls .upper() on each word.",
          criteria: ['.upper() is applied inside the comprehension', 'Every word in the result is fully uppercase'],
          hint: '[word.upper() for word in words].',
          solution: 'A comprehension calling .upper() on each word.' },
      ] },
    { no: 10, week: 4, session: 1, title: 'Modules & Imports', video_url: 'https://www.youtube.com/watch?v=CqvZ3vGoGs0', topic: 'Import standard libraries like math or random to leverage built-in functionality.\n\nimport math\nfrom random import randint\nprint(math.sqrt(16))\nprint(randint(1, 10))',
      problems: [
        { title: 'Math Module', points: 30, difficulty: 'Basic',
          description: "The built-in math module provides ready-made functions like sqrt() so you don't have to write your own square root calculation. Write a program that imports the math module and calculates the square root of 144 using math.sqrt(144).",
          criteria: ['math is imported', 'math.sqrt(144) produces 12.0'],
          hint: 'import math; math.sqrt(144).',
          solution: 'import math then math.sqrt(144) printed.' },
        { title: 'Random Int', points: 30, difficulty: 'Basic',
          description: "The random module's randint() function generates a genuinely random whole number within a range you specify, rather than a fixed hard-coded value. Write a program that imports the random module and generates a random integer between 1 and 100 using random.randint(1, 100).",
          criteria: ['random is imported', 'random.randint(1, 100) is used, not a hard-coded number'],
          hint: 'import random; random.randint(1, 100).',
          solution: 'import random then randint(1, 100) printed.' },
        { title: 'Module Alias', points: 40, difficulty: 'Core',
          description: "The as keyword lets you import a module under a shorter alias, which is common practice for frequently used modules. Write a program that imports a module using an alias (e.g. import math as m), and actually uses that alias to call something from the module, such as m.pi.",
          criteria: ['The import uses the as keyword', 'The alias is actually used to call something from the module'],
          hint: 'import math as m; m.pi.',
          solution: 'import math as m, then m.pi (or similar) used and printed.' },
      ] },
  ],
},

{
  key: 'cs105-javascript', course_code: 'CS-105', free: true, friendly_grading: true, default_language: 'web',
  title: 'CS-105: Fundamentals of JavaScript',
  description: 'Event-driven scripting language of the web covering ES6+ semantics, functional array workflows, DOM manipulation, and promises. Ten topics, three tasks each, certificate at the end.',
  outcome: 'Understand ECMAScript standards, dynamic typing, and DOM manipulation basics; implement modular asynchronous event handling utilizing arrow functions, callbacks, and array operations; master non-blocking asynchronous execution using Promises, async/await, and fetch().',
  keywords: ['fundamentals of JavaScript', 'basics of JavaScript', 'free JavaScript course', 'learn JavaScript online', 'JavaScript for beginners'],
  key_concepts: ['let/const/var and scope', 'Data types and coercion', 'Arrow functions', 'Objects and literals', 'Array methods (map/filter/reduce)', 'Template literals and destructuring', 'DOM selection', 'Event listeners', 'Promises', 'async/await and fetch()'],
  pass_mark: 60, titleNames: ['Script Starter', 'Async Architect'],
  levels: [
    { no: 1, week: 1, session: 1, title: 'Variables (let, const, var)', video_url: 'https://www.youtube.com/watch?v=iqjs3yNN5zo', topic: 'Modern JavaScript utilizes block-scoped let and const declarations instead of function-scoped var.\n\nlet count = 1;\nconst MAX = 100;\nif (true) {\n  let blockScoped = "Hidden";\n}',
      problems: [
        { title: 'Const Assignment', points: 30, difficulty: 'Basic',
          description: "A variable declared with const can never be reassigned after its initial value is set - attempting to do so throws a TypeError at runtime. Write code that declares a const variable, then attempts to reassign it, catching and logging the resulting TypeError so the failure is visible in the output.",
          criteria: ['A const variable is declared', 'The code demonstrates/logs that reassigning it throws a TypeError'],
          hint: 'const x = 10; x = 20; throws a TypeError.',
          solution: 'Reassignment attempt wrapped in try/catch, error logged.' },
        { title: 'Block Scope', points: 30, difficulty: 'Basic',
          description: "A let declaration is scoped to the nearest enclosing block ({ }), so it simply doesn't exist once you step outside that block, unlike var. Write code that declares a let inside an if block, then demonstrates that trying to access it outside that block fails.",
          criteria: ['let is declared inside a block', 'Accessing it outside the block is shown to fail'],
          hint: 'Block scope prevents access from outside.',
          solution: 'A let inside {} demonstrated as inaccessible outside, via try/catch or console output.' },
        { title: 'Hoisting Behavior', points: 40, difficulty: 'Core',
          description: "Variables declared with var are hoisted to the top of their scope, so referencing one before its declaration line doesn't throw an error - it just logs undefined. Write code that logs a var-declared variable before the line where it's actually assigned, and observe that the result is undefined rather than a crash.",
          criteria: ['var is used (not let/const) for this demo', 'The logged value is undefined, not an error'],
          hint: 'Returns undefined due to hoisting.',
          solution: 'console.log(x) before var x = ...; logs undefined.' },
      ] },
    { no: 2, week: 1, session: 2, title: 'Data Types & Coercion', video_url: 'https://www.youtube.com/watch?v=OPZ1wfqsqrs', topic: "JS supports primitive types and converts matching types automatically during loose comparison evaluation.\n\nconsole.log('5' == 5); // true (coercion)\nconsole.log('5' === 5); // false (strict)\nconsole.log(typeof 42); // 'number'",
      problems: [
        { title: 'Strict Equality', points: 30, difficulty: 'Basic',
          description: "== compares values after converting types to match (coercion), while === compares both value and type without any conversion - the same two operands can give different answers depending on which you use. Write code that logs the result of '5' == 5 and then '5' === 5, so both the coerced and strict comparisons are visible.",
          criteria: ['Both comparisons are logged', '== logs true and === logs false'],
          hint: '=== checks both value and type.',
          solution: "console.log('5' == 5) then console.log('5' === 5), true then false." },
        { title: 'Type Inspection', points: 30, difficulty: 'Basic',
          description: "The typeof operator reports what kind of value a variable actually holds at runtime, which is useful for debugging dynamically-typed code. Write code that uses typeof on at least two values of different types (e.g. a string and a number), and logs each result.",
          criteria: ['typeof is used on at least two different values', 'Each logged type matches the actual value'],
          hint: "typeof 'hello' returns 'string'.",
          solution: 'typeof calls logged for a string and a number.' },
        { title: 'Number Casting', points: 40, difficulty: 'Core',
          description: "parseInt() reads the leading numeric digits out of a string and ignores whatever comes after, which makes it useful for values like CSS measurements that mix numbers and units. Write code that casts the string '42px' using parseInt(), and logs the result to confirm it comes out as the number 42, not NaN.",
          criteria: ['parseInt() is used, not Number()', 'The result is the number 42, not NaN'],
          hint: "parseInt('42px') parses the leading digits.",
          solution: "parseInt('42px') logged as 42." },
      ] },
    { no: 3, week: 1, session: 3, title: 'Arrow Functions', video_url: 'https://www.youtube.com/watch?v=LiwAKSvrfr0', topic: 'Arrow functions () => {} offer concise syntax and preserve lexical context binding.\n\nconst double = x => x * 2;\nconst add = (a, b) => a + b;\nconsole.log(double(5));',
      problems: [
        { title: 'Convert Function', points: 30, difficulty: 'Basic',
          description: "Arrow functions are a shorter way to write a function expression, using => instead of the function keyword. Write an arrow function version of a simple two-argument function such as add(a, b), and call it to confirm it produces the correct result.",
          criteria: ['The function is written as an arrow function', 'Calling it produces the correct result'],
          hint: 'const add = (a, b) => a + b;.',
          solution: 'An arrow function replacing an equivalent function declaration.' },
        { title: 'Implicit Return', points: 30, difficulty: 'Basic',
          description: "When an arrow function's body is a single expression with no curly braces, that expression's value is returned automatically, with no return keyword needed. Write an arrow function that squares a number using an implicit return (no curly braces, no return keyword), e.g. const square = x => x * x;.",
          criteria: ['Curly braces are omitted', 'The expression value is returned implicitly'],
          hint: 'Omit curly braces for an implicit return.',
          solution: 'const square = x => x * x; with no braces or return.' },
        { title: 'Default Parameters', points: 40, difficulty: 'Core',
          description: "A default parameter value lets an arrow function fall back to a preset value whenever the caller leaves that argument out. Write an arrow function with a default parameter value (e.g. (name = 'User') => ...), and call it both with and without supplying that argument.",
          criteria: ['A default value is set in the parameter list', 'Calling with no argument uses the default'],
          hint: "(name = 'User') => ...",
          solution: 'Arrow function with a default parameter, called both ways.' },
      ] },
    { no: 4, week: 2, session: 1, title: 'Objects & Literals', video_url: 'https://www.youtube.com/watch?v=7d9H34ZVRPg', topic: 'JavaScript objects are key-value structures storing properties and methods.\n\nconst user = {\n  name: "Alice",\n  greet() { console.log("Hi"); }\n};\nuser.greet();',
      problems: [
        { title: 'Object Creation', points: 30, difficulty: 'Basic',
          description: "Object literals ({ }) are the simplest way to group related data under one variable, storing each piece as a named property. Write code that creates a user object with name and age properties using an object literal, and logs both properties correctly.",
          criteria: ["const user = { name: 'Alice', age: 25 }; shape is used", 'Both properties are logged correctly'],
          hint: "const user = { name: 'Alice', age: 25 };.",
          solution: 'An object literal with two properties, both logged.' },
        { title: 'Property Modification', points: 30, difficulty: 'Basic',
          description: "An object's properties can be read and updated using either dot notation (obj.prop) or bracket notation (obj['prop']), and both work interchangeably. Write code that updates at least one property using dot notation and one using bracket notation, then logs the updated values.",
          criteria: ['Both dot and bracket notation are used at least once', 'The updated values are logged afterward'],
          hint: "user.age = 26; user['name'] = 'Bob';.",
          solution: 'Properties updated via dot and bracket access, then logged.' },
        { title: 'Object Method', points: 40, difficulty: 'Core',
          description: "A method is simply a property whose value happens to be a function, letting an object carry behavior alongside its data. Write code that attaches a method (a function-valued property) to an object, and calls it with object.method() to confirm it runs correctly.",
          criteria: ['The method is a property whose value is a function', 'Calling object.method() runs it correctly'],
          hint: 'Place the function as an object property value.',
          solution: 'An object with a method property, invoked and logging correctly.' },
      ] },
    { no: 5, week: 2, session: 2, title: 'Array Methods (map, filter)', video_url: 'https://www.youtube.com/watch?v=8MoElay6dWU', topic: 'Process array data functionally using non-mutating higher-order methods.\n\nconst nums = [1, 2, 3, 4];\nconst doubled = nums.map(n => n * 2);\nconst evens = nums.filter(n => n % 2 === 0);',
      problems: [
        { title: 'Array Map', points: 30, difficulty: 'Basic',
          description: ".map() builds a brand-new array by applying a transformation to every element of an existing array, without a manual loop or mutating the original. Write code that doubles every number in an array using .map(), and logs the resulting array.",
          criteria: ['.map() is used, not a manual loop', 'Every value in the result is exactly double the original'],
          hint: 'nums.map(n => n * 2).',
          solution: '.map() producing a doubled array, logged.' },
        { title: 'Array Filter', points: 30, difficulty: 'Basic',
          description: ".filter() builds a new array containing only the elements that pass a test you supply, discarding the rest. Write code that removes the odd numbers from an array using .filter(), keeping only the even values, and logs the result.",
          criteria: ['.filter() is used', 'Only even numbers remain in the result'],
          hint: 'nums.filter(n => n % 2 === 0).',
          solution: '.filter() keeping only even values, logged.' },
        { title: 'Array Reduce', points: 40, difficulty: 'Core',
          description: ".reduce() collapses an entire array down to a single value by repeatedly combining an accumulator with each element, starting from an initial value you provide. Write code that sums every number in an array using .reduce() with an initial accumulator of 0, and logs the final total.",
          criteria: ['.reduce() is used with an initial value of 0', 'The final sum is numerically correct'],
          hint: 'nums.reduce((acc, curr) => acc + curr, 0).',
          solution: '.reduce() accumulating a correct total, logged.' },
      ] },
    { no: 6, week: 2, session: 3, title: 'Template Literals', video_url: 'https://www.youtube.com/watch?v=52OJhTbCtoA', topic: 'Backticks `${var}` simplify multi-line string construction and string interpolation.\n\nconst name = "World";\nconst msg = `Hello ${name}!`;\nconst [a, b] = [10, 20]; // Destructuring',
      problems: [
        { title: 'Interpolation', points: 30, difficulty: 'Basic',
          description: "Template literals, written with backticks, let you embed variables directly inside a string using ${ } instead of stitching pieces together with +. Write code that builds a dynamic string interpolating two variables inside a backtick template literal, and logs the result.",
          criteria: ['Backticks and ${} are used', 'Both variables appear correctly interpolated in the output'],
          hint: '`Hello ${firstName} ${lastName}`.',
          solution: 'A backtick template string interpolating two variables.' },
        { title: 'Object Destructuring', points: 40, difficulty: 'Core',
          description: "Destructuring lets you pull specific properties straight out of an object into their own variables in a single line, instead of accessing them one by one. Write code that extracts the title and price properties from an object using destructuring syntax, e.g. const { title, price } = book;, and logs both values.",
          criteria: ['Destructuring syntax { title, price } = obj is used', 'Both extracted values are logged correctly'],
          hint: 'const { title, price } = book;.',
          solution: 'Object destructuring pulling two named properties out.' },
        { title: 'Array Destructuring', points: 30, difficulty: 'Basic',
          description: "Array destructuring works the same way as object destructuring but matches by position instead of by name, pulling elements out in order. Write code that extracts the first two elements of an array using const [first, second] = array;, and logs both to confirm they match the correct positions.",
          criteria: ['const [first, second] = array; syntax is used', 'Both values match the correct array positions'],
          hint: 'const [first, second] = array;',
          solution: 'Array destructuring pulling the first two elements.' },
      ] },
    { no: 7, week: 3, session: 1, title: 'DOM Selection', video_url: 'https://www.youtube.com/watch?v=JlgLDfINXvY', topic: "Query and manipulate page elements dynamically using document.querySelector().\n\nconst el = document.querySelector('#title');\nel.textContent = 'Updated Title';\nel.classList.add('active');",
      problems: [
        { title: 'Target Element', points: 30, difficulty: 'Basic',
          description: "document.querySelector() finds the first element on the page matching a CSS-style selector, such as an ID prefixed with #. Write HTML containing an element with id=\"title\", then write JavaScript that selects it using document.querySelector('#title').",
          criteria: ["An element with id=\"title\" exists in the HTML", "document.querySelector('#title') selects it"],
          hint: "const el = document.querySelector('#title');.",
          solution: 'A matching element in the markup, correctly selected in JS.' },
        { title: 'Update Text', points: 30, difficulty: 'Basic',
          description: ".textContent lets you replace an element's visible text safely, without risking the HTML-injection issues that come with using innerHTML for plain text. Write code that changes an element's visible text using .textContent, and confirm the change is actually visible on the page.",
          criteria: ['.textContent is used, not innerHTML', 'The visible text actually changes'],
          hint: "el.textContent = 'New Heading';.",
          solution: '.textContent assignment visibly updating the element.' },
        { title: 'Toggle Classes', points: 40, difficulty: 'Core',
          description: ".classList.toggle() adds a CSS class if it's missing and removes it if it's already present, which is the standard way to flip a visual state on and off. Write code that toggles a CSS class on an element using .classList.toggle(), and pair it with a style rule so the toggle produces a visible change.",
          criteria: ['.classList.toggle() is used', 'The class visibly appears/disappears when toggled'],
          hint: "el.classList.toggle('active');.",
          solution: '.classList.toggle() flipping a class, with a visible style tied to it.' },
      ] },
    { no: 8, week: 3, session: 2, title: 'Event Listeners', video_url: 'https://www.youtube.com/watch?v=BqN2LsMASLY', topic: "Attach event handlers to DOM elements to trigger interactive functions on click or keyup.\n\nconst btn = document.querySelector('button');\nbtn.addEventListener('click', (e) => {\n  console.log('Button clicked!');\n});",
      problems: [
        { title: 'Click Handler', points: 30, difficulty: 'Basic',
          description: "addEventListener('click', ...) hooks a function up to run automatically whenever a specific element is clicked, without needing an inline onclick attribute. Write code that attaches a click listener to a button using addEventListener('click', ...), producing a visible or logged effect when it's clicked.",
          criteria: ["addEventListener('click', ...) is used", 'Clicking the button produces a visible/logged effect'],
          hint: "btn.addEventListener('click', ...).",
          solution: 'A working click handler with an observable effect.' },
        { title: 'Form Submit', points: 40, difficulty: 'Core',
          description: "Submitting a form normally reloads the page by default, which event.preventDefault() stops so you can handle the submission with your own JavaScript instead. Write a submit event handler on a form that calls event.preventDefault() before doing anything else, so the page does not reload or navigate away.",
          criteria: ['event.preventDefault() is called', 'The page does not reload/navigate on submit'],
          hint: 'event.preventDefault().',
          solution: 'A submit handler calling preventDefault() before anything else.' },
        { title: 'Keypress Tracker', points: 40, difficulty: 'Core',
          description: "A keyup listener fires every time the user releases a key, and the event's e.key property tells you exactly which key was pressed. Write code that attaches a keyup listener to an input, reads e.key inside the callback, and logs it for every keypress.",
          criteria: ['A keyup listener is attached', 'e.key is read and logged for each press'],
          hint: 'Read the e.key property inside the keyup callback.',
          solution: "addEventListener('keyup', e => console.log(e.key))." },
      ] },
    { no: 9, week: 3, session: 3, title: 'Promises', video_url: 'https://www.youtube.com/watch?v=TnhCX0KkPqs', topic: "Promises represent background asynchronous operation states (pending, fulfilled, rejected).\n\nconst p = new Promise((resolve) => {\n  setTimeout(() => resolve('Loaded'), 1000);\n});\np.then(data => console.log(data));",
      problems: [
        { title: 'Create Promise', points: 30, difficulty: 'Basic',
          description: "A Promise represents a value that isn't ready yet but will be at some point, constructed with new Promise((resolve, reject) => ...) and resolved by calling resolve() with the eventual value. Write code that creates a new Promise which eventually resolves with a text value (for example after a setTimeout), using new Promise((resolve, reject) => ...).",
          criteria: ['new Promise((resolve, reject) => ...) is used', 'It eventually resolves with a real value'],
          hint: 'new Promise((resolve, reject) => ...).',
          solution: 'A Promise constructed and resolved with a string.' },
        { title: 'Consume Promise', points: 30, difficulty: 'Basic',
          description: ".then() attaches a callback that runs once a Promise successfully resolves, receiving the resolved value as its argument. Write code that chains a .then() handler onto a promise, logging the resolved value inside that handler.",
          criteria: ['.then() is chained onto the promise', 'The resolved value is logged inside it'],
          hint: '.then(data => console.log(data)).',
          solution: '.then() logging the resolved value.' },
        { title: 'Catch Errors', points: 40, difficulty: 'Core',
          description: ".catch() attaches a handler specifically for when a Promise rejects, letting you deal with the failure instead of letting it crash silently. Write code with a promise that rejects, and a .catch() chained after .then() that logs the error without crashing the program.",
          criteria: ['.catch() is chained after .then()', 'A rejected promise is logged without crashing'],
          hint: '.catch(err => console.error(err)).',
          solution: 'A promise that rejects, caught and logged via .catch().' },
      ] },
    { no: 10, week: 4, session: 1, title: 'async / await & fetch()', video_url: 'https://www.youtube.com/watch?v=Yp9KIcSKTNo', topic: "async/await provides synchronous-style syntax to handle Promises and fetch API responses.\n\nasync function getData() {\n  const res = await fetch('https://api.example.com');\n  const data = await res.json();\n}",
      problems: [
        { title: 'Async Function', points: 30, difficulty: 'Basic',
          description: "Marking a function async is what unlocks the ability to use await inside it, letting asynchronous code read in a more sequential, synchronous style. Write a function declared with the async keyword, containing at least one await expression inside its body.",
          criteria: ['The function is declared with the async keyword', 'It can use await inside its body'],
          hint: 'async function getData() {}.',
          solution: 'An async function declaration containing at least one await.' },
        { title: 'Fetch Data', points: 40, difficulty: 'Core',
          description: "await fetch(url), used inside an async function, pauses execution until the network request completes and hands back the response object directly, instead of requiring a separate .then() chain. Write an async function that fetches a URL using await fetch(url), and stores the response in a variable.",
          criteria: ['await fetch(url) is used inside an async function', 'The response is captured in a variable'],
          hint: 'const res = await fetch(url);.',
          solution: 'await fetch(url) inside an async function, response stored.' },
        { title: 'Parse JSON', points: 40, difficulty: 'Core',
          description: "A fetch response's body isn't usable data yet until you call its .json() method, which itself returns a promise that needs to be awaited too. Write an async function that extracts the JSON payload from a fetch response using await res.json(), and logs or otherwise uses the parsed data.",
          criteria: ['await res.json() is used', 'The parsed data is logged or used afterward'],
          hint: 'const data = await res.json();.',
          solution: 'await res.json() producing a usable JS object, logged.' },
      ] },
  ],
},

{
  key: 'cs106-css', course_code: 'CS-106', free: true, friendly_grading: true, default_language: 'web',
  title: 'CS-106: Fundamentals of CSS',
  description: 'Visual presentation layer styling HTML documents, box model layout mathematics, Flexbox, Grid, and media responsiveness. Ten topics, three tasks each, certificate at the end.',
  outcome: 'Master CSS selectors, cascade priority rules, and specificity; manipulate layout geometry accurately using the CSS Box Model, Flexbox, and Grid systems; build responsive web experiences using CSS media queries.',
  keywords: ['fundamentals of CSS', 'basics of CSS', 'free CSS course', 'learn CSS online', 'CSS for beginners'],
  key_concepts: ['Selectors', 'Specificity and cascade', 'The box model', 'Colors and backgrounds', 'Typography', 'Display property', 'Positioning', 'Flexbox', 'Grid', 'Responsive media queries'],
  pass_mark: 60, titleNames: ['Style Starter', 'Layout Architect'],
  levels: [
    { no: 1, week: 1, session: 1, title: 'Selectors & CSS Inclusion', video_url: 'https://www.youtube.com/watch?v=WC7WurHBGs0', topic: 'CSS targets HTML elements using tag names, classes (.), or unique IDs (#).\n\n/* External or Internal CSS */\nh1 { color: navy; }\n.highlight { background: yellow; }\n#main { padding: 10px; }',
      problems: [
        { title: 'Class Selector', points: 30, difficulty: 'Basic',
          description: "A class selector, written with a leading dot, targets every element carrying that class name regardless of its tag. Write a CSS rule using a class selector (e.g. .highlight { }) that sets yellow text color, and apply that class to an element so its text turns yellow.",
          criteria: ['A .highlight { } class rule is defined', 'The element using that class shows yellow text'],
          hint: '.highlight { color: yellow; }.',
          solution: 'A class selector with color: yellow applied to a matching element.' },
        { title: 'Group Selectors', points: 30, difficulty: 'Basic',
          description: "Listing multiple selectors separated by commas lets one CSS rule apply the same styles to several different elements at once, instead of repeating the rule. Write a single CSS rule that styles both h1 and h2 elements simultaneously using a comma-separated selector, e.g. h1, h2 { }.",
          criteria: ['A single comma-separated selector targets both', 'Both heading levels visibly share the style'],
          hint: 'h1, h2 { font-family: sans-serif; }.',
          solution: 'One grouped selector applying to both tags at once.' },
        { title: 'Descendant Selector', points: 40, difficulty: 'Core',
          description: "A descendant selector (written as two selectors separated by a space) only targets elements nested inside another specific element, leaving elements elsewhere on the page untouched. Write a CSS rule using a descendant selector (div span { }) that only styles span elements nested inside a div, not standalone spans elsewhere on the page.",
          criteria: ['The selector is written as div span', 'Only spans nested inside a div are affected, not standalone spans'],
          hint: 'div span { ... }.',
          solution: 'A descendant combinator scoping the rule correctly.' },
      ] },
    { no: 2, week: 1, session: 2, title: 'Specificity & Cascade', video_url: 'https://www.youtube.com/watch?v=uTcpbPMZlFE', topic: 'The cascade resolves conflicting style rules based on Inline > ID > Class > Element priority.\n\n/* ID overrides Class rules */\np { color: black; }\n.text { color: blue; }\n#unique { color: red; } /* Wins */',
      problems: [
        { title: 'Override Rule', points: 30, difficulty: 'Basic',
          description: "When two rules target the same element, the more specific selector wins - and an ID selector is more specific than a plain tag selector. Write both a tag-level rule (e.g. h1 { }) and an ID rule (e.g. #main-title { }) that set conflicting values for the same property on the same element, and confirm the ID rule visibly wins.",
          criteria: ['Both a tag rule and an ID rule target the same element', 'The ID rule visibly wins'],
          hint: '#main-title overrides a generic h1 rule.',
          solution: 'An ID selector rule that visibly beats an element selector rule.' },
        { title: 'Force Priority', points: 30, difficulty: 'Basic',
          description: "!important overrides the normal specificity rules entirely, forcing a declaration to win even against a rule that would otherwise take priority. Write a CSS rule with lower specificity than another rule targeting the same property, but add !important to it so it wins anyway.",
          criteria: ['!important is appended before the semicolon', 'It wins over a rule that would otherwise have higher specificity'],
          hint: 'Append !important before the semicolon.',
          solution: 'A lower-specificity rule with !important beating a higher-specificity one.' },
        { title: 'Equal Specificity', points: 40, difficulty: 'Core',
          description: "When two rules have exactly equal specificity, the cascade falls back to source order - whichever rule appears later in the stylesheet wins. Write two CSS rules with identical specificity that target the same property on the same element, and demonstrate that the one defined later in the file is the one that actually takes effect.",
          criteria: ['Two rules with equal specificity target the same property', 'The one defined later in the file wins, as shown by the result'],
          hint: 'The rule defined lowest in the file wins.',
          solution: 'Two equally-specific rules, with the later one visibly taking effect.' },
      ] },
    { no: 3, week: 1, session: 3, title: 'CSS Box Model', video_url: 'https://www.youtube.com/watch?v=Xrxd6cEajhM', topic: 'Elements render as rectangular boxes consisting of content, padding, border, and margin.\n\n.box {\n  width: 200px;\n  padding: 20px;\n  border: 2px solid black;\n  box-sizing: border-box;\n}',
      problems: [
        { title: 'Add Padding', points: 30, difficulty: 'Basic',
          description: "Padding is the space inside an element's border, between the border and its content, controlled with the padding property. Write a CSS rule that applies 20px of padding to an element, so the content visibly sits inset from the element's edges.",
          criteria: ['padding: 20px; is applied', 'The content is visibly inset from the border'],
          hint: 'padding: 20px;.',
          solution: 'A box with visible interior spacing from its edges.' },
        { title: 'Add Margin', points: 30, difficulty: 'Basic',
          description: "Setting a fixed width together with margin: 0 auto; is the classic technique for centering a block element horizontally within its container. Write a CSS rule that gives an element a fixed width and centers it horizontally using margin: 0 auto;.",
          criteria: ['The element has a fixed width', 'margin: 0 auto; centers it'],
          hint: 'margin: 0 auto; with a fixed width.',
          solution: 'A fixed-width block centered via auto side margins.' },
        { title: 'Box Sizing', points: 40, difficulty: 'Core',
          description: "By default, padding and border are added on top of an element's declared width, but box-sizing: border-box; folds them inside it instead, keeping the total width exact. Write a CSS rule that sets box-sizing: border-box; on an element with padding and a border, and confirm the element doesn't expand beyond its declared width.",
          criteria: ['box-sizing: border-box; is set', 'The element does not expand beyond its declared width when padding/border are added'],
          hint: 'Prevents the element from expanding beyond its declared width.',
          solution: 'border-box applied, width stays exact including padding/border.' },
      ] },
    { no: 4, week: 2, session: 1, title: 'Colors & Backgrounds', video_url: 'https://www.youtube.com/watch?v=rQPRrPcIz5w', topic: 'Define visual styling using Hex values, RGBA opacity, or background cover images.\n\n.card {\n  background-color: #f4f4f4;\n  color: rgba(0, 0, 0, 0.8);\n  background-size: cover;\n}',
      problems: [
        { title: 'Hex Colors', points: 30, difficulty: 'Basic',
          description: "Hex color codes (like #f4f4f4) are a compact way to specify an exact color using six hexadecimal digits. Write a CSS rule that sets an element's background-color using a valid 6-digit hex code, and confirm the background visibly changes to that color.",
          criteria: ['A valid 6-digit hex code is used', 'The background visibly changes to that color'],
          hint: 'background-color: #f4f4f4;.',
          solution: 'A hex background-color applied and visible.' },
        { title: 'RGB Transparency', points: 30, difficulty: 'Basic',
          description: "The rgba() function adds a fourth alpha value to a color, controlling how transparent it appears - 0.5 gives you a color at 50% opacity. Write a CSS rule that sets a background color using rgba() with an alpha value of 0.5, so the element renders semi-transparent.",
          criteria: ['rgba() is used with an alpha of 0.5', 'The element is visibly semi-transparent'],
          hint: 'background-color: rgba(0,0,0,0.5);.',
          solution: 'rgba() with 0.5 alpha, visibly translucent.' },
        { title: 'Background Cover', points: 40, difficulty: 'Core',
          description: "background-size: cover; scales a background image so it always fills its container completely, cropping the image if needed rather than leaving empty gaps. Write a CSS rule with a background image and background-size: cover;, so the image fills its container without visible gaps.",
          criteria: ['background-size: cover; is set', 'The image fills the container without visible gaps'],
          hint: 'background-size: cover;.',
          solution: 'A background image with cover sizing, filling its box.' },
      ] },
    { no: 5, week: 2, session: 2, title: 'Typography & Fonts', video_url: 'https://www.youtube.com/watch?v=RNakAX3rVVw', topic: 'Format font families, relative rem font sizes, line heights, and web font stacks.\n\nbody {\n  font-family: Arial, sans-serif;\n  font-size: 1rem;\n  line-height: 1.6;\n}',
      problems: [
        { title: 'Font Stack', points: 30, difficulty: 'Basic',
          description: "A font stack lists several fonts in order of preference, ending in a generic family, so the browser falls back gracefully if the first choice isn't available. Write a font-family declaration listing at least two specific fonts followed by a generic fallback like sans-serif, e.g. font-family: Arial, sans-serif;.",
          criteria: ['At least two fonts are listed, comma-separated', 'A generic family (e.g. sans-serif) is the final fallback'],
          hint: 'font-family: Arial, sans-serif;.',
          solution: 'A font-family list ending in a generic fallback.' },
        { title: 'Relative Font Sizes', points: 30, difficulty: 'Basic',
          description: "rem units size text relative to the root element's font size, rather than a fixed pixel value, so everything scales together if the root size ever changes. Write a CSS rule that sets font-size using rem units instead of px, so the text visibly scales relative to the root font size.",
          criteria: ['rem units are used, not fixed px', 'Text visibly scales relative to the root size'],
          hint: 'font-size: 1.5rem;.',
          solution: 'font-size set in rem, scaling relative to root.' },
        { title: 'Line Height', points: 40, difficulty: 'Core',
          description: "line-height controls the vertical spacing between lines of text, and increasing it above the default (roughly 1.2) makes paragraphs noticeably easier to read. Write a CSS rule that sets a paragraph's line-height to a value like 1.6, and confirm the visible spacing between lines increases.",
          criteria: ['line-height is set to a value above the default (~1.2)', 'The visible spacing between lines increases'],
          hint: 'line-height: 1.6;.',
          solution: 'line-height: 1.6 applied, visibly looser line spacing.' },
      ] },
    { no: 6, week: 2, session: 3, title: 'Display Property', video_url: 'https://www.youtube.com/watch?v=hRHV5cjEB1w', topic: 'Control layout rendering behaviors using block, inline, inline-block, or display: none.\n\n.nav-item { display: inline-block; }\n.hidden { display: none; }\n.full-box { display: block; }',
      problems: [
        { title: 'Inline Block', points: 30, difficulty: 'Basic',
          description: "An inline element like a link normally ignores width and height settings, but display: inline-block; lets it flow inline with text while still respecting box dimensions. Write a CSS rule that sets display: inline-block; on a link, and confirm it now respects an explicit width and height.",
          criteria: ['display: inline-block; is applied', 'The element now respects an explicit width/height'],
          hint: 'display: inline-block;.',
          solution: 'An inline element made inline-block, now sized correctly.' },
        { title: 'Hide Element', points: 30, difficulty: 'Basic',
          description: "display: none; doesn't just make an element invisible - it removes it from the page layout entirely, so it takes up no space at all. Write a CSS rule that applies display: none; to an element, and confirm it disappears completely and takes up no layout space.",
          criteria: ['display: none; is applied', 'The element takes up no space, not just invisible'],
          hint: 'display: none;.',
          solution: 'display: none removing the element from layout.' },
        { title: 'Block Conversion', points: 40, difficulty: 'Core',
          description: "A span is inline by default, sitting alongside other content on the same line, but display: block; forces it to occupy its own full-width line like a div would. Write a CSS rule that applies display: block; to a span, and confirm it now occupies its own line instead of sitting inline.",
          criteria: ['display: block; is applied to a span', 'It now occupies its own full-width line'],
          hint: 'display: block;.',
          solution: 'A span forced to block display, now stacking on its own line.' },
      ] },
    { no: 7, week: 3, session: 1, title: 'CSS Positioning', video_url: 'https://www.youtube.com/watch?v=h_Smqpqs_1k', topic: 'Position elements relative to normal flow or absolutely anchored inside relative containers.\n\n.parent { position: relative; }\n.badge {\n  position: absolute;\n  top: 0; right: 0;\n}',
      problems: [
        { title: 'Relative Anchor', points: 30, difficulty: 'Basic',
          description: "Setting position: relative; on a parent element doesn't move the parent itself, but it creates the anchor point that any absolutely-positioned children will be measured against. Write a CSS rule that sets position: relative; on a parent element, so it can serve as the anchor for an absolutely positioned child.",
          criteria: ['position: relative; is set on the parent', 'It serves as the anchor for an absolutely positioned child'],
          hint: 'position: relative;.',
          solution: 'A parent set to position: relative, anchoring its children.' },
        { title: 'Absolute Badge', points: 40, difficulty: 'Core',
          description: "position: absolute; pulls an element out of normal flow and positions it using top/right/bottom/left offsets, measured against its nearest positioned ancestor. Write a CSS rule that positions a badge element at the top-right corner of its container using position: absolute; top: 0; right: 0;.",
          criteria: ['position: absolute; top: 0; right: 0; is applied', 'The badge visibly sits in the top-right corner'],
          hint: 'position: absolute; top: 0; right: 0;.',
          solution: 'An absolutely positioned badge anchored to the top-right.' },
        { title: 'Fixed Header', points: 40, difficulty: 'Core',
          description: "position: fixed; anchors an element to the browser viewport itself rather than any container, so it stays in place even as the rest of the page scrolls. Write a CSS rule that pins a header to the top of the viewport using position: fixed; top: 0; width: 100%;, and confirm it stays visible while the page scrolls.",
          criteria: ['position: fixed; top: 0; width: 100%; is applied', 'The header stays visible while the page scrolls'],
          hint: 'position: fixed; top: 0; width: 100%;.',
          solution: 'A fixed-position header that stays put during scroll.' },
      ] },
    { no: 8, week: 3, session: 2, title: 'Flexbox Core Layout', video_url: 'https://www.youtube.com/watch?v=wsTv9y931o8', topic: 'Flexbox simplifies 1D layout alignment along horizontal rows or vertical columns.\n\n.container {\n  display: flex;\n  justify-content: center;\n  align-items: center;\n}',
      problems: [
        { title: 'Enable Flex', points: 30, difficulty: 'Basic',
          description: "Setting display: flex; on a container turns all of its direct children into flex items that lay out in a row by default, without any floats or manual positioning. Write a CSS rule that sets display: flex; on a container, and confirm its direct children now flow in a flex row.",
          criteria: ['display: flex; is set on the container', 'Its direct children now lay out in a flex row'],
          hint: 'display: flex;.',
          solution: 'A container with display: flex, children flowing in a row.' },
        { title: 'Center Axis', points: 30, difficulty: 'Basic',
          description: "justify-content controls alignment along the main axis and align-items controls the cross axis, and setting both to center centers items in both directions at once. Write CSS that sets justify-content: center; and align-items: center; on a flex container, so its items appear centered both horizontally and vertically.",
          criteria: ['justify-content: center; and align-items: center; are both set', 'Items are visibly centered both horizontally and vertically'],
          hint: 'justify-content: center; align-items: center;.',
          solution: 'Both centering properties applied, items centered in both directions.' },
        { title: 'Flex Direction', points: 40, difficulty: 'Core',
          description: "flex-direction: column; switches a flex container's main axis from horizontal to vertical, stacking its items top to bottom instead of side by side. Write a CSS rule that sets flex-direction: column; on a flex container, so its items visibly stack vertically instead of horizontally.",
          criteria: ['flex-direction: column; is set', 'Items visibly stack top to bottom instead of side by side'],
          hint: 'flex-direction: column;.',
          solution: 'flex-direction: column reorienting the flex items.' },
      ] },
    { no: 9, week: 3, session: 3, title: 'CSS Grid Basics', video_url: 'https://www.youtube.com/watch?v=EiNiSFIPIQE', topic: 'CSS Grid structures 2D page layouts organized across defined rows and columns.\n\n.grid {\n  display: grid;\n  grid-template-columns: repeat(3, 1fr);\n  gap: 15px;\n}',
      problems: [
        { title: 'Enable Grid', points: 30, difficulty: 'Basic',
          description: "CSS Grid's grid-template-columns property defines how many columns a grid has and how wide each one is - repeat(3, 1fr) creates three equal-width columns in one line. Write CSS that sets display: grid; and grid-template-columns: repeat(3, 1fr); on a container, producing three equal-width columns.",
          criteria: ['display: grid; is set', 'grid-template-columns: repeat(3, 1fr); creates three equal columns'],
          hint: 'grid-template-columns: repeat(3, 1fr);.',
          solution: 'A grid container with three equal-width columns.' },
        { title: 'Grid Gap', points: 30, difficulty: 'Basic',
          description: "The gap property adds consistent spacing between every row and column of a grid, without needing margins on individual cells. Write a CSS rule that sets gap: 15px; on a grid container, so visible spacing appears between every cell.",
          criteria: ['gap: 15px; is set on the grid container', 'Visible spacing appears between every cell'],
          hint: 'gap: 15px;.',
          solution: 'gap: 15px applied, visible spacing between cells.' },
        { title: 'Grid Column Span', points: 40, difficulty: 'Core',
          description: "grid-column: span 2; lets a single grid item stretch across multiple columns instead of occupying just one, useful for featured or header cells. Write a CSS rule that applies grid-column: span 2; to one grid item, so it visibly occupies two columns' worth of width while the rest occupy one.",
          criteria: ['grid-column: span 2; is applied to one item', 'That item visibly occupies two columns\' worth of width'],
          hint: 'grid-column: span 2;.',
          solution: 'One item spanning two columns while the rest occupy one.' },
      ] },
    { no: 10, week: 4, session: 1, title: 'Responsive Media Queries', video_url: 'https://www.youtube.com/watch?v=69IbzTWg5PM', topic: 'Media queries apply conditional CSS styling depending on viewport device width.\n\n@media (max-width: 768px) {\n  .container { flex-direction: column; }\n  .sidebar { display: none; }\n}',
      problems: [
        { title: 'Mobile Breakpoint', points: 30, difficulty: 'Basic',
          description: "A media query applies CSS conditionally based on the viewport's width, letting you write rules that only take effect below a certain screen size. Write a media query using @media (max-width: 768px) { }, with at least one rule inside it that only applies on screens narrower than that.",
          criteria: ['@media (max-width: 768px) { } is used', 'At least one rule inside it only applies below that width'],
          hint: '@media (max-width: 768px) { ... }.',
          solution: 'A media query block scoped to narrow viewports.' },
        { title: 'Mobile Stack', points: 40, difficulty: 'Core',
          description: "Combining flex-direction with a media query lets a layout reflow from a row on desktop to a stacked column on narrow screens, without duplicating the whole layout. Write CSS that sets flex-direction: column; inside a @media (max-width: 768px) query, so the layout switches to a vertical stack only on narrow screens.",
          criteria: ['flex-direction: column; is set inside the media query', 'The layout visibly stacks vertically only on narrow screens'],
          hint: 'flex-direction: column; inside the query.',
          solution: 'flex-direction: column nested inside the media query.' },
        { title: 'Hide Sidebar', points: 40, difficulty: 'Core',
          description: "Nesting display: none; inside a media query hides an element only below a certain screen width, leaving it visible everywhere else. Write CSS that hides a .sidebar element with display: none; nested inside a @media (max-width: 768px) query, so the sidebar disappears only on mobile screens.",
          criteria: ['.sidebar { display: none; } is inside the media query', 'The sidebar disappears only below the breakpoint'],
          hint: '.sidebar { display: none; } inside the query.',
          solution: 'A sidebar hidden specifically within the mobile media query.' },
      ] },
  ],
},

{
  key: 'cs107-html', course_code: 'CS-107', free: true, friendly_grading: true, default_language: 'web',
  title: 'CS-107: Fundamentals of HTML',
  description: 'Standard markup backbone defining page hierarchy, semantic document structures, embedded media, forms, and accessibility. Ten topics, three tasks each, certificate at the end.',
  outcome: 'Structure semantic, accessible HTML documents according to current standards; embed multi-format media elements using correct attributes; construct semantic HTML forms and tables using correct accessibility attributes.',
  keywords: ['fundamentals of HTML', 'basics of HTML', 'free HTML course', 'learn HTML online', 'HTML for beginners'],
  key_concepts: ['Document structure and DOCTYPE', 'Headings and paragraphs', 'Text formatting elements', 'Lists', 'Links and anchors', 'Images and media', 'Tables', 'Forms and inputs', 'Form labels and accessibility', 'Semantic HTML5 elements'],
  pass_mark: 60, titleNames: ['Markup Starter', 'Semantic Builder'],
  levels: [
    { no: 1, week: 1, session: 1, title: 'Document Structure & DOCTYPE', video_url: 'https://www.youtube.com/watch?v=9nmFfPucWAw', topic: 'HTML documents build upon a standard skeleton containing DOCTYPE, html, head, and body tags.\n\n<!DOCTYPE html>\n<html>\n<head>\n  <title>My Portal</title>\n</head>\n<body>\n  <h1>Welcome</h1>\n</body>\n</html>',
      problems: [
        { title: 'HTML Skeleton', points: 30, difficulty: 'Basic',
          description: "Every HTML document needs the same basic skeleton - a DOCTYPE declaration plus properly nested html, head, and body tags - before any real content goes in. Write a complete boilerplate HTML document starting with <!DOCTYPE html>, with <html>, <head>, and <body> all present and correctly nested.",
          criteria: ['<!DOCTYPE html> is present', '<html>, <head>, and <body> are all present and properly nested'],
          hint: 'Essential tags: <!DOCTYPE html><html>...</html>.',
          solution: 'A complete, valid HTML skeleton.' },
        { title: 'Page Title', points: 30, difficulty: 'Basic',
          description: "The <title> element controls what shows up in the browser tab, and it must live inside <head>, not <body>, since it isn't part of the visible page content. Write an HTML document with a <title> element placed correctly inside <head>, containing real page-title text.",
          criteria: ['<title> is inside <head>, not <body>', 'It contains real page-title text'],
          hint: '<title>My Portal</title>.',
          solution: 'A <title> element correctly placed inside <head>.' },
        { title: 'Character Encoding', points: 40, difficulty: 'Core',
          description: "A charset meta tag tells the browser how to interpret the bytes of the page as text, and UTF-8 is the standard choice that supports virtually every character. Write an HTML document with a <meta charset=\"UTF-8\"> tag placed inside <head>.",
          criteria: ['<meta charset="UTF-8"> is present', 'It is placed inside <head>'],
          hint: '<meta charset="UTF-8">.',
          solution: 'A charset meta tag correctly placed in the document head.' },
      ] },
    { no: 2, week: 1, session: 2, title: 'Headings & Paragraph Tags', video_url: 'https://www.youtube.com/watch?v=GHmpcvYuMWs', topic: 'Structure document text hierarchy using headings (h1 to h6) and paragraphs (p).\n\n<h1>Main Heading</h1>\n<h2>Sub Heading</h2>\n<p>Paragraph text block.<br>New line text.</p>\n<hr>',
      problems: [
        { title: 'Heading Hierarchy', points: 30, difficulty: 'Basic',
          description: "Heading tags (h1 through h6) establish a document's text hierarchy, and they should be used in descending order so the structure makes semantic sense. Write HTML containing an h1, an h2, and an h3, appearing in that descending order on the page.",
          criteria: ['All three heading levels are present', 'They appear in descending order (h1 then h2 then h3)'],
          hint: 'Keep heading levels ordered.',
          solution: 'h1, h2, h3 used in the correct sequence.' },
        { title: 'Paragraph Content', points: 30, difficulty: 'Basic',
          description: "The <br> tag forces a line break inside a block of text without starting a whole new paragraph, which <p> alone can't do. Write a paragraph wrapped in <p> that contains a <br> tag, producing a visible line break inside that single paragraph.",
          criteria: ['Text is wrapped in <p>', 'A <br> creates a visible line break inside it'],
          hint: 'Wrap the text in <p> and insert <br>.',
          solution: 'A <p> containing a <br> splitting two lines of text.' },
        { title: 'Horizontal Divider', points: 40, difficulty: 'Core',
          description: "The self-closing <hr> tag draws a horizontal dividing line across the page, commonly used to visually separate sections of content. Write HTML containing a single self-closing <hr> tag, and confirm it renders as a visible divider.",
          criteria: ['A self-closing <hr> tag is used', 'It renders as a visible dividing line'],
          hint: 'Insert a self-closing <hr> tag.',
          solution: 'A single <hr> element rendering a divider.' },
      ] },
    { no: 3, week: 1, session: 3, title: 'Text Formatting Elements', video_url: 'https://www.youtube.com/watch?v=ElTNDaw7-Vs', topic: 'Format inline content semantically using strong, em, mark, and code tags.\n\n<p>This is <strong>important</strong> and <em>emphasized</em> text.</p>\n<code>const x = 10;</code>',
      problems: [
        { title: 'Bold Text', points: 30, difficulty: 'Basic',
          description: "<strong> marks text as semantically important (and renders it bold), which is the more meaningful choice over the purely visual <b> tag. Write a paragraph that wraps a piece of text in <strong> instead of <b>, and confirm it renders visually bold.",
          criteria: ['<strong> is used, not <b>', 'The text renders visually bold'],
          hint: 'Use <strong> instead of <b>.',
          solution: '<strong> wrapping the important text.' },
        { title: 'Code Display', points: 30, difficulty: 'Basic',
          description: "The <code> tag marks up a snippet as literal code, which browsers render in a monospace font by default to visually set it apart from normal text. Write HTML that wraps a short code snippet in <code> tags, and confirm it renders in a monospace style.",
          criteria: ['<code> wraps the snippet', 'It renders in a monospace style'],
          hint: 'Enclose the code in <code> tags.',
          solution: 'A <code> element wrapping literal code text.' },
        { title: 'Highlighted Text', points: 40, difficulty: 'Core',
          description: "The <mark> tag semantically highlights a piece of text, and browsers render it with a visible background color by default, similar to a highlighter pen. Write HTML that wraps a piece of text in <mark> tags, and confirm it renders with a visible highlight background.",
          criteria: ['<mark> wraps the target text', 'It renders with a visible highlight background'],
          hint: 'Wrap the target text in <mark> tags.',
          solution: 'A <mark> element visibly highlighting text.' },
      ] },
    { no: 4, week: 2, session: 1, title: 'Lists (ul, ol)', video_url: 'https://www.youtube.com/watch?v=fWBqwz9Nju8', topic: 'Group related content items into bulleted unordered lists or numbered ordered lists.\n\n<ul>\n  <li>Item 1</li>\n  <li>Item 2</li>\n</ul>\n<ol>\n  <li>Step 1</li>\n</ol>',
      problems: [
        { title: 'Unordered List', points: 30, difficulty: 'Basic',
          description: "An unordered list (<ul>) groups related items together and displays each one as a bullet point, with each item wrapped in its own <li> tag. Write HTML that builds a bulleted list using <ul> containing at least two <li> items, and confirm they render as bullets.",
          criteria: ['<ul> wraps one or more <li> items', 'Items render as bullets'],
          hint: '<ul><li>Item</li></ul>.',
          solution: 'A <ul> with multiple <li> items.' },
        { title: 'Ordered List', points: 30, difficulty: 'Basic',
          description: "An ordered list (<ol>) works just like <ul> but numbers each item automatically, which is useful for sequential steps or rankings. Write HTML that builds a numbered list using <ol> containing at least one <li> item, and confirm it renders with sequential numbers.",
          criteria: ['<ol> wraps one or more <li> items', 'Items render with sequential numbers'],
          hint: '<ol><li>Step 1</li></ol>.',
          solution: 'An <ol> with multiple <li> items, auto-numbered.' },
        { title: 'Nested Lists', points: 40, difficulty: 'Core',
          description: "A list can be nested inside a single <li> of another list, creating a sub-list that renders indented beneath its parent item. Write HTML that places a <ul> directly inside one <li> of an outer list, and confirm the nested list renders indented under its parent item.",
          criteria: ['A <ul> is placed directly inside a parent <li>', 'The nested list renders indented under its parent item'],
          hint: 'Place the <ul> directly inside the parent <li> tag.',
          solution: 'A nested <ul> living inside one <li> of an outer list.' },
      ] },
    { no: 5, week: 2, session: 2, title: 'Links & Anchors (a)', video_url: 'https://www.youtube.com/watch?v=81wMe4sWGVQ', topic: 'Hyperlinks (a) connect documents using target URLs supplied to the href attribute.\n\n<a href="https://google.com" target="_blank">Search</a>\n<a href="#footer">Jump to Footer</a>',
      problems: [
        { title: 'External Link', points: 30, difficulty: 'Basic',
          description: "The <a> tag creates a hyperlink, with the destination URL supplied through its href attribute. Write HTML that creates a link navigating to https://google.com using <a href=\"https://google.com\">, with visible, clickable link text.",
          criteria: ['<a href="https://google.com"> is used', 'The link text is visible and clickable'],
          hint: '<a href="https://google.com">Search</a>.',
          solution: 'A working external <a> link.' },
        { title: 'New Tab Target', points: 30, difficulty: 'Basic',
          description: "Adding target=\"_blank\" to a link makes it open in a new browser tab instead of navigating away from the current page. Write an <a> link that includes both a valid href and target=\"_blank\", so clicking it opens in a new tab.",
          criteria: ['target="_blank" is added to the link', 'The link still has a valid href'],
          hint: 'Add the target="_blank" attribute.',
          solution: 'An <a> with target="_blank" alongside its href.' },
        { title: 'Internal Bookmark', points: 40, difficulty: 'Core',
          description: "A link whose href starts with # jumps to the element on the same page whose id matches that value, without leaving the page - useful for jump-to-section navigation. Write HTML with a link whose href is #footer, plus an element with id=\"footer\" elsewhere on the page for it to jump to.",
          criteria: ['The link href is #footer', 'An element with id="footer" exists on the page to jump to'],
          hint: '<a href="#footer">Jump to Footer</a>.',
          solution: 'A hash-link paired with a matching id="footer" target element.' },
      ] },
    { no: 6, week: 2, session: 3, title: 'Images & Media', video_url: 'https://www.youtube.com/watch?v=ktJFw-FKOOA', topic: 'Embed graphics, video players, and audio streams natively using media tags.\n\n<img src="logo.png" alt="Logo">\n<video src="clip.mp4" controls></video>\n<audio src="song.mp3" controls></audio>',
      problems: [
        { title: 'Embed Image', points: 30, difficulty: 'Basic',
          description: "The <img> tag embeds a picture using its src attribute, and the alt attribute provides descriptive fallback text for screen readers or if the image fails to load. Write HTML that embeds an image using <img> with both a src and a meaningful (non-empty) alt attribute.",
          criteria: ['<img> has both src and alt attributes', 'alt text is descriptive, not empty'],
          hint: '<img src="logo.png" alt="Logo">.',
          solution: 'An <img> with a meaningful alt attribute.' },
        { title: 'Embed Video', points: 30, difficulty: 'Basic',
          description: "The <video> tag embeds a native video player, and adding the controls attribute shows the browser's built-in play/pause/volume controls. Write HTML that embeds a video using <video> with a src and the controls attribute, so playback controls are visible.",
          criteria: ['<video> has a src and the controls attribute', 'Playback controls are visible'],
          hint: '<video src="clip.mp4" controls></video>.',
          solution: 'A <video> element with controls enabled.' },
        { title: 'Audio Element', points: 40, difficulty: 'Core',
          description: "The <audio> tag works like <video> but for sound, and combining the controls and loop attributes gives a playable player that repeats automatically. Write HTML that embeds an audio player using <audio> with both controls and loop attributes set.",
          criteria: ['<audio> has both controls and loop attributes', 'It is set up to repeat playback'],
          hint: '<audio src="music.mp3" controls loop></audio>.',
          solution: 'An <audio> element with both controls and loop set.' },
      ] },
    { no: 7, week: 3, session: 1, title: 'Tables & Data Display', video_url: 'https://www.youtube.com/watch?v=eM0UgWpUV8g', topic: 'Structure tabular datasets into organized rows and columns using table semantic tags.\n\n<table>\n  <thead><tr><th>Name</th><th>Score</th></tr></thead>\n  <tbody><tr><td>Alice</td><td>95</td></tr></tbody>\n</table>',
      problems: [
        { title: 'Basic Table', points: 30, difficulty: 'Basic',
          description: "A table is built from <tr> rows containing <th> header cells or <td> data cells, nested inside a <table> element. Write HTML that builds a 2x2 table with Name and Score columns, using <tr> for rows and <th>/<td> for the cells.",
          criteria: ['<tr> defines rows, <th>/<td> define cells', 'The table renders 2 rows and 2 columns correctly'],
          hint: 'Use <tr> for rows, <th> and <td> for cells.',
          solution: 'A correctly structured 2x2 <table>.' },
        { title: 'Colspan Merge', points: 40, difficulty: 'Core',
          description: "The colspan attribute lets a single table cell stretch across multiple columns, merging what would otherwise be separate cells. Write a table where one <th> uses colspan=\"2\" to visibly merge across two column widths.",
          criteria: ['colspan="2" is applied to one <th>', 'It visibly merges across two column widths'],
          hint: '<th colspan="2">Merged Header</th>.',
          solution: 'A <th> with colspan="2" spanning two columns.' },
        { title: 'Semantic Sections', points: 40, difficulty: 'Core',
          description: "Wrapping header rows in <thead> and data rows in <tbody> gives a table clearer semantic structure, separating its labels from its actual content. Write a table where the header row(s) are wrapped in <thead> and the data row(s) are wrapped in <tbody>.",
          criteria: ['Header row(s) are wrapped in <thead>', 'Data row(s) are wrapped in <tbody>'],
          hint: 'Wrap headers in <thead>, rows in <tbody>.',
          solution: 'A table properly split into <thead> and <tbody> sections.' },
      ] },
    { no: 8, week: 3, session: 2, title: 'Forms & Inputs', video_url: 'https://www.youtube.com/watch?v=MKSQYsLLFEo', topic: 'Collect user input using dynamic input types, checkboxes, radio buttons, and form tags.\n\n<form action="/submit" method="POST">\n  <input type="email" required>\n  <button type="submit">Submit</button>\n</form>',
      problems: [
        { title: 'Login Form', points: 30, difficulty: 'Basic',
          description: "Dedicated input types like type=\"email\" and type=\"password\" give the browser useful hints for validation and masking, compared to a generic text input. Write a form containing an <input type=\"email\"> and an <input type=\"password\">, forming a basic login form.",
          criteria: ['<input type="email"> is present', '<input type="password"> is present'],
          hint: '<input type="email"> and <input type="password">.',
          solution: 'A form with an email input and a password input.' },
        { title: 'Radio Group', points: 30, difficulty: 'Basic',
          description: "Radio buttons only behave as a mutually-exclusive group when they all share the exact same name attribute - without that, each one can be selected independently. Write HTML with multiple <input type=\"radio\"> elements sharing the same name attribute, so selecting one deselects the others.",
          criteria: ['Multiple <input type="radio"> share the same name attribute', 'Selecting one deselects the others'],
          hint: 'Assign identical name attributes to all radios in the group.',
          solution: 'A radio group sharing one name, mutually exclusive.' },
        { title: 'Form Action', points: 40, difficulty: 'Core',
          description: "A form's action attribute tells the browser where to send the submitted data, and method controls how it's sent - POST is the standard choice for data that changes something on the server. Write a <form> with action=\"/submit\" and method=\"POST\" both set.",
          criteria: ['action="/submit" is set on the form', 'method="POST" is set on the form'],
          hint: '<form action="/submit" method="POST">.',
          solution: 'A <form> with both action and method="POST" set correctly.' },
      ] },
    { no: 9, week: 3, session: 3, title: 'Form Labels & Accessibility', video_url: 'https://www.youtube.com/watch?v=6xq57uhKJSA', topic: 'Associate text descriptions with input elements using matching for and id attributes.\n\n<label for="user">Username:</label>\n<input id="user" type="text" placeholder="Enter username" required>',
      problems: [
        { title: 'Label Binding', points: 30, difficulty: 'Basic',
          description: "A <label>'s for attribute must exactly match an input's id to create a real accessible connection between them - clicking the label then focuses the input automatically. Write HTML with a <label for=\"...\"> whose value exactly matches an input's id attribute, and confirm clicking the label focuses the input.",
          criteria: ['<label for="..."> matches the input\'s id exactly', 'Clicking the label focuses the input'],
          hint: '<label for="user">User</label><input id="user">.',
          solution: 'A label/input pair correctly matched by for/id.' },
        { title: 'Required Inputs', points: 30, difficulty: 'Basic',
          description: "The boolean required attribute tells the browser to block form submission until that field has been filled in, without needing any custom JavaScript validation. Write an input with the required attribute set, and confirm the browser blocks submission when the form is submitted empty.",
          criteria: ['The boolean required attribute is present', 'Submitting the form empty is blocked by the browser'],
          hint: 'Add the boolean required attribute.',
          solution: 'An input with required, blocking empty submission.' },
        { title: 'Placeholder Text', points: 40, difficulty: 'Core',
          description: "The placeholder attribute shows light grey hint text inside an empty input, which disappears the moment the user starts typing. Write an input with a placeholder attribute containing genuinely helpful hint text, e.g. placeholder=\"Enter your email\".",
          criteria: ['placeholder="..." is set with genuinely helpful hint text', 'It disappears once the user starts typing'],
          hint: 'placeholder="Enter your email".',
          solution: 'An input with clear, helpful placeholder text.' },
      ] },
    { no: 10, week: 4, session: 1, title: 'Semantic HTML5 Elements', video_url: 'https://www.youtube.com/watch?v=xGYz9yIXOz4', topic: 'Replace generic div wrappers with semantic structural elements describing content meaning.\n\n<header><h1>Title</h1></header>\n<nav><a href="/">Home</a></nav>\n<main><article><p>Content</p></article></main>\n<footer>Footer Text</footer>',
      problems: [
        { title: 'Page Skeleton', points: 30, difficulty: 'Basic',
          description: "Semantic elements like <header>, <main>, and <footer> describe what a section of the page actually is, which plain <div>s never can, improving both accessibility and readability. Write an HTML page skeleton using <header>, <main>, and <footer> instead of generic container divs doing the same job.",
          criteria: ['<header>, <main>, and <footer> are all present', 'No container divs remain doing the same job'],
          hint: 'Replace the container divs with semantic tags.',
          solution: 'A page skeleton using header/main/footer instead of divs.' },
        { title: 'Navigation Section', points: 40, difficulty: 'Core',
          description: "The <nav> element semantically marks a section of the page as navigation, which helps browsers, search engines, and assistive technology understand its purpose. Write HTML that wraps a set of navigation links inside a <nav> element, with real, working <a> links inside it.",
          criteria: ['<nav> wraps the navigation links', 'The links inside are real, working anchors'],
          hint: 'Place the navigation links inside a <nav> tag.',
          solution: 'A <nav> containing working navigation <a> links.' },
        { title: 'Article Container', points: 40, difficulty: 'Core',
          description: "The <article> element marks up a genuinely self-contained piece of content - like a blog post or news item - that would make sense on its own, and is typically placed inside <main>. Write HTML with an <article> element inside <main>, containing one self-contained piece of content.",
          criteria: ['<article> wraps a genuinely self-contained piece of content', 'It sits inside <main>'],
          hint: 'Wrap the article content within <article> elements.',
          solution: 'An <article> inside <main>, holding one self-contained piece of content.' },
      ] },
  ],
},

];
