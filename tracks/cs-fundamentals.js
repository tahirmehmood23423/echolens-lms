'use strict';
/** EchoLens tracks - CS Fundamentals free courses (CS-104..CS-106).
 * Companions to CS-101 (C, fc01-c-basics) and CS-102 (C++ with OOP,
 * fc02-cpp-objects) in tracks/free-micro.js - together the full 5-track
 * curriculum: C, C++ (OOP folded into Module 3), Python, JavaScript, and
 * HTML & CSS (combined into one track, since CSS can't be taught without
 * the HTML underneath it). Same shape as every other track file (free:
 * true -> auto-certificate on completion above the pass mark).
 * friendly_grading: true (see store.js's OpenQuest.applyGrade and
 * server.js's /api/open/submit) - all five tracks skip the standard 10%
 * AI-grading reduction and never surface automated-grading or failure
 * wording to the student; a first-timer's first working program shouldn't
 * come with a visible penalty or a "the grader is broken" scare.
 * CS-104 (Fundamentals of Python) now follows its official syllabus PDF
 * verbatim, in the same shape as FC-01/FC-02: five modules, fifteen topics,
 * one real-life analogy + concept note + a real YouTube video + one
 * scenario-based Compiler Quest per topic, each with an exact Sample Input
 * / Expected Output so the grader has a literal target. video_url is still
 * intentionally left null for CS-105 (JavaScript) and CS-106 (HTML & CSS)
 * until their video links and matching PDFs are sourced too.
 */

module.exports = [

{
  key: 'cs104-python', course_code: 'CS-104', free: true, friendly_grading: true, default_language: 'python',
  title: 'CS-104: Fundamentals of Python',
  description: 'Absolute beginner to working Python - the highest-demand first language, from the bytecode interpreter and dynamic typing through strings, control flow and core data structures to functions, classes and safe file I/O. Five modules, fifteen topics, one real-life analogy, one video and one scenario-based Compiler Quest each, every quest with an exact Sample Input and Expected Output - certificate at the end.',
  outcome: 'Use the Python interpreter and print() formatting, dynamic variables and input() casting to drive an interactive program; slice strings and build f-strings, and branch and loop over data with if/elif/else and for/while; model data with lists, comprehensions, tuples, sets and dictionaries; write functions with default and variable arguments and lambdas; design classes with __init__, inheritance and method overriding, and read and write files safely with with and try/except.',
  keywords: ['fundamentals of Python', 'basics of Python programming', 'free Python course', 'learn Python online', 'Python for beginners', 'Python interpreter and print', 'Python dynamic typing', 'Python classes and inheritance'],
  key_concepts: ['Python interpreter & print()', 'Dynamic variables & types', 'input() & type casting', 'String slicing & f-strings', 'Decision structures (if / elif / else)', 'Iteration with for & while loops', 'Lists & list comprehensions', 'Tuples & sets', 'Dictionaries & key-value lookups', 'Functions & default arguments', '*args, **kwargs & variable arguments', 'Lambda expressions & scope (LEGB)', 'Classes, objects & __init__', 'Inheritance & polymorphism', "File handling with 'with' & exceptions"],
  pass_mark: 60, titleNames: ['Python Starter', 'Pythonista'],
  levels: [
    { no: 1, week: 1, session: 1, title: 'Python Interpreter & print()', video_url: 'https://www.youtube.com/watch?v=kqtD5dpn9C8',
      topic: "Real-life analogy: Think of a live simultaneous translator reading a speech line-by-line, speaking each sentence as it arrives - never waiting for the whole book to be printed and bound first.\n\nPython runs on a bytecode interpreter that executes your source one statement at a time, so there is no separate compile-and-link step to wait through. The print() function sends formatted text to standard output; its sep parameter sets what goes between multiple arguments (a space by default) and its end parameter sets what finishes the line (a newline by default).",
      problems: [
        { title: 'System Diagnostic Banner', points: 30, difficulty: 'Basic',
          description: "Print a startup message, then on the next line print the three diagnostic tags CPU, MEM and DISK joined by a dash using print()'s sep parameter - the way a machine prints a boot banner and self-check before it comes online.\n\nInput: None\nExpected Output:\nBooting Python Engine\nCPU-MEM-DISK",
          criteria: ['The three tags are joined with print(\"CPU\", \"MEM\", \"DISK\", sep=\"-\"), not a hand-typed \"CPU-MEM-DISK\" string or + concatenation', 'The output is exactly two lines: \"Booting Python Engine\" then \"CPU-MEM-DISK\", nothing more'],
          hint: 'print("Booting Python Engine") then print("CPU", "MEM", "DISK", sep="-").',
          solution: 'Two print() calls, the second using sep="-" to join the three diagnostic tags into CPU-MEM-DISK.' },
      ] },
    { no: 2, week: 1, session: 2, title: 'Dynamic Variables & Types', video_url: 'https://www.youtube.com/watch?v=cQT33yu9pY8',
      topic: "Real-life analogy: Think of a universal storage bin. You peel off the old label sticker and put on a new one whenever you want - today it holds shoes, tomorrow books, the day after coins - the bin never cares what type of thing goes in.\n\nPython variables are names that refer to objects on the heap, and the same name can be rebound to a value of a different type at any time - you never declare a type. The core primitives are int (whole numbers), float (decimals) and bool (True / False), and type(x) reports which one an object is.",
      problems: [
        { title: 'Inventory Price Multiplier', points: 30, difficulty: 'Basic',
          description: "Read an integer item count and a float unit price from one line separated by a space. Multiply them, then print the total and its runtime type using type() - exactly as a till multiplies quantity by price and the result lands in a decimal money type.\n\nSample Input: 4 12.5\nExpected Output: Total: 50.0 | Type: <class 'float'>",
          criteria: ['The count is converted with int() and the price with float() before multiplying', "The type is printed with type(total), producing the literal <class 'float'>, not a hand-typed string"],
          hint: 'count, price = input().split(); then total = int(count) * float(price).',
          solution: "An int count times a float price giving 50.0, with type(total) printing <class 'float'>." },
      ] },
    { no: 3, week: 1, session: 3, title: 'User Input with input() & Type Casting', video_url: 'https://www.youtube.com/watch?v=61a7UkDO50s',
      topic: "Real-life analogy: Think of a microphone capturing raw audio as text. Before a synthesizer can play it back as musical notes, that text has to be converted into numerical pitch frequencies - the raw capture alone is not a number.\n\ninput() always returns a str, no matter what the user types. Before any arithmetic you must cast it with int() or float(); skipping the cast either raises a TypeError or silently does string work (\"20\" * 3) instead of maths.",
      problems: [
        { title: 'Age Verification Scanner', points: 40, difficulty: 'Core',
          description: "Read a name and a birth year separated by a space. Convert the birth year to an int, compute the age assuming the current year is 2026, and print the summary line - the same check a sign-up form runs before it lets someone register.\n\nSample Input: Ali 2004\nExpected Output: User Ali is 22 years old.",
          criteria: ['The birth year is converted with int() before the subtraction (2026 - year), never left as a string', 'For input \"Ali 2004\" the program prints exactly \"User Ali is 22 years old.\"'],
          hint: 'name, year = input().split(); age = 2026 - int(year).',
          solution: 'An int-cast birth year subtracted from 2026, printed inside the exact summary sentence (22 for 2004).' },
      ] },
    { no: 4, week: 2, session: 1, title: 'String Slicing & f-Strings', video_url: 'https://www.youtube.com/watch?v=k9TUPpGqYTo',
      topic: "Real-life analogy: Think of cutting one specific rectangular frame out of a long film strip (slicing), then laying a dynamic subtitle over that frame (an f-string).\n\nStrings are immutable sequences. The slice str[start:stop:step] returns a new substring without changing the original, and an f-string f\"{value}\" drops variables straight into text - faster and clearer than + concatenation.",
      problems: [
        { title: 'Product SKU Code Parser', points: 40, difficulty: 'Core',
          description: "Read a SKU string in the form SKU-9481-PKG. Slice out just the 4-digit product ID in the middle and print it behind a # using an f-string - the way a warehouse scanner pulls the item number out of a longer barcode label.\n\nSample Input: SKU-9481-PKG\nExpected Output: Parsed Product ID: #9481",
          criteria: ['The 4 digits are extracted with slice notation (e.g. sku[4:8]), not a hard-coded \"9481\"', 'The result is printed via an f-string, producing exactly \"Parsed Product ID: #9481\"'],
          hint: 'sku[4:8] grabs the four characters after "SKU-"; then print(f"Parsed Product ID: #{pid}").',
          solution: 'A slice pulling the middle 4 digits from the SKU, printed behind # with an f-string.' },
      ] },
    { no: 5, week: 2, session: 2, title: 'Decision Structures (if, elif, else)', video_url: 'https://www.youtube.com/watch?v=DZwmZ8Usvnk',
      topic: "Real-life analogy: Think of an automated postal sorting machine dropping each letter into the local, domestic or international chute based on its postcode - one letter, exactly one chute.\n\nif tests a condition; elif checks the next one only when every earlier test failed; else catches everything left over. Conditions use comparison operators (< > == !=) and can be combined with the logical operators and, or and not.",
      problems: [
        { title: 'Speed Radar Enforcer', points: 30, difficulty: 'Basic',
          description: "Read a single integer speed. Print NORMAL if it is 80 or below, WARNING if it is 81 to 100, and PENALTY if it is above 100 - the exact three-band decision a roadside speed camera makes on every car.\n\nSample Input: 95\nExpected Output: WARNING",
          criteria: ['The three outcomes form one if / elif / else chain, not three separate independent if statements', 'For input 95 the program prints exactly \"WARNING\", and only one band ever prints per input'],
          hint: 'if speed <= 80: ... elif speed <= 100: ... else: ...',
          solution: 'A chained if/elif/else printing exactly one of NORMAL, WARNING or PENALTY - WARNING for 95.' },
      ] },
    { no: 6, week: 2, session: 3, title: 'Iteration with for & while Loops', video_url: 'https://www.youtube.com/watch?v=6iF8Xb7Z3wQ',
      topic: "Real-life analogy: Think of a quality-control conveyor belt that examines every box in turn, and keeps going until someone hits the emergency stop button.\n\nA for loop steps over a range or a collection a known number of times; a while loop repeats as long as its condition stays true. break leaves the loop early and continue skips straight to the next iteration.",
      problems: [
        { title: 'Even Sequence Accumulator', points: 30, difficulty: 'Basic',
          description: "Read a single integer N. Loop through 1 to N with range(), add up only the even numbers, and print the total - a meter that ticks forward only on every second unit.\n\nSample Input: 10\nExpected Output: Even Sum: 30",
          criteria: ['A loop over range() walks 1..N and an even test (n % 2 == 0) decides what gets added', 'For N = 10 the program prints exactly \"Even Sum: 30\" (2 + 4 + 6 + 8 + 10)'],
          hint: 'total = 0; for n in range(1, N + 1): if n % 2 == 0: total += n.',
          solution: 'A range() loop summing only the even values from 1 to N, printing Even Sum: 30 for N = 10.' },
      ] },
    { no: 7, week: 3, session: 1, title: 'Lists & List Comprehensions', video_url: 'https://www.youtube.com/watch?v=W8KRzm-HUcc',
      topic: "Real-life analogy: Think of a digital shopping cart you can add to, remove from, reorder, or run a bulk discount across in a single pass.\n\nA list is a mutable, ordered sequence. A comprehension [expr for x in iterable if cond] builds a whole new list in one readable line, replacing the append-inside-a-loop pattern.",
      problems: [
        { title: 'Squares of Odds Generator', points: 40, difficulty: 'Core',
          description: "Read a line of space-separated integers. Using a single list comprehension, square only the odd values and print the resulting list - one pass over the belt that keeps the odd items and transforms them.\n\nSample Input: 1 2 3 4\nExpected Output: [1, 9]",
          criteria: ['One list comprehension does both the odd filter (if n % 2) and the squaring (n ** 2) - no manual loop with .append()', 'For input \"1 2 3 4\" the program prints exactly \"[1, 9]\"'],
          hint: '[n ** 2 for n in nums if n % 2].',
          solution: 'A single comprehension filtering odds and squaring them, printing [1, 9] for 1 2 3 4.' },
      ] },
    { no: 8, week: 3, session: 2, title: 'Tuples & Sets', video_url: 'https://www.youtube.com/watch?v=W8KRzm-HUcc',
      topic: "Real-life analogy: Think of a sealed birth certificate that can never be edited (a tuple), and an automated party guestbook that silently refuses to write down a name it already has (a set).\n\nA tuple (x, y) is an immutable record - fixed the moment it is created. A set {a, b} stores only unique values and offers O(1) membership tests plus set algebra (union, intersection, difference).",
      problems: [
        { title: 'Duplicate Identifier Filter', points: 40, difficulty: 'Core',
          description: "Read a line of space-separated integers that may contain repeats. Convert to a set to drop the duplicates, then print the unique count and the sorted list of unique values - a turnstile that counts each distinct badge only once.\n\nSample Input: 5 2 5 8 2\nExpected Output: Unique Count: 3 | Unique Elements: [2, 5, 8]",
          criteria: ['Duplicates are removed by converting to a set, not by a manual membership loop', 'The unique values are passed through sorted() for display, printing exactly \"Unique Count: 3 | Unique Elements: [2, 5, 8]\"'],
          hint: 'uniq = set(nums); then len(uniq) and sorted(uniq).',
          solution: 'A set dropping the repeats, with len() and sorted() giving Unique Count: 3 and [2, 5, 8].' },
      ] },
    { no: 9, week: 3, session: 3, title: 'Dictionaries & Key-Value Lookups', video_url: 'https://www.youtube.com/watch?v=daefaLgNkw0',
      topic: "Real-life analogy: Think of a physical telephone directory - you look up a contact name and get their number instantly, without scanning every page one by one.\n\nA dictionary {key: value} stores pairs and finds a value by key in near-constant time. .get() reads safely with a fallback, and .keys(), .values() and .items() let you iterate the parts.",
      problems: [
        { title: 'Word Frequency Counter', points: 40, difficulty: 'Core',
          description: "Read a line of text. Split it into words, tally how many times each word appears in a dictionary, and print the dictionary - the count a search index keeps for every term on a page.\n\nSample Input: apple banana apple\nExpected Output: {'apple': 2, 'banana': 1}",
          criteria: ['Counts are built in a dict, incrementing per word (d[w] = d.get(w, 0) + 1 or similar) - not a fixed set of named variables', "For input \"apple banana apple\" the program prints exactly \"{'apple': 2, 'banana': 1}\""],
          hint: 'for w in text.split(): freq[w] = freq.get(w, 0) + 1.',
          solution: "A dict tallying word counts, printing {'apple': 2, 'banana': 1} for the sample line." },
      ] },
    { no: 10, week: 4, session: 1, title: 'Defining Functions & Default Arguments', video_url: 'https://www.youtube.com/watch?v=9Os0o3wzS_I',
      topic: "Real-life analogy: Think of a coffee vending machine with recipe buttons. Press Espresso and it uses the standard recipe; or optionally dial in a custom sugar level before you press.\n\ndef names a reusable block that takes parameters and hands back a result with return. A default argument (people=2) makes a parameter optional, and defaults must come after all the required parameters in the signature.",
      problems: [
        { title: 'Bill Splitter Function', points: 40, difficulty: 'Core',
          description: "Write a function split_bill(amount, people=2) that returns each person's share. Read an amount and a people count, call the function, and print the per-person share formatted to 2 decimal places - the same split a group does over a restaurant bill.\n\nSample Input: 150 3\nExpected Output: Share per person: $50.00",
          criteria: ['split_bill is a def with people defaulting to 2 (def split_bill(amount, people=2)), and the share is returned, not printed inside the function', 'The share is formatted to exactly 2 decimals, printing \"Share per person: $50.00\" for input \"150 3\"'],
          hint: 'def split_bill(amount, people=2): return amount / people  - then print(f"Share per person: ${share:.2f}").',
          solution: 'A split_bill(amount, people=2) function whose returned share prints as $50.00 for 150 split 3 ways.' },
      ] },
    { no: 11, week: 4, session: 2, title: '*args, **kwargs & Variable Arguments', video_url: 'https://www.youtube.com/watch?v=WcdaLwO4Kbg',
      topic: "Real-life analogy: Think of an open grocery bag that stretches to hold any number of items (*args), plus a box of labelled gift tags where each tag has a name and a value (**kwargs).\n\n*args collects any extra positional arguments into a tuple; **kwargs collects any extra named arguments into a dict. Together they let one function accept a variable number of inputs.",
      problems: [
        { title: 'Dynamic Math Product Engine', points: 40, difficulty: 'Core',
          description: "Write a function product_all(*numbers) that multiplies every argument it receives and returns the total. Read a line of space-separated integers, unpack them into the call, and print the combined product - a till that multiplies however many quantities you hand it.\n\nSample Input: 2 3 4 5\nExpected Output: Combined Product: 120",
          criteria: ['product_all is defined with *numbers and works for any count of arguments, not a fixed a, b, c, d list', 'For input \"2 3 4 5\" the program prints exactly \"Combined Product: 120\"'],
          hint: 'def product_all(*numbers): total = 1; for n in numbers: total *= n; return total  - call product_all(*nums).',
          solution: 'A product_all(*numbers) function multiplying all arguments, returning 120 for 2 3 4 5.' },
      ] },
    { no: 12, week: 4, session: 3, title: 'Lambda Expressions & Scope (LEGB)', video_url: 'https://www.youtube.com/watch?v=25ovCm9jKfA',
      topic: "Real-life analogy: Think of a disposable sticky-note calculation you use once and throw away - and you always check your own desk notes before walking over to the office noticeboard.\n\nA lambda is a small anonymous function written inline: lambda x: x * 2. Name lookup follows LEGB - Local, then Enclosing, then Global, then Built-in - so an inner scope can see outer names but not the other way round.",
      problems: [
        { title: 'Custom Key Sorter', points: 40, difficulty: 'Core',
          description: "Read pairs of name and age from one line (name age name age ...). Build a list of (name, age) tuples and sort it by age ascending using a lambda key in sorted(), then print the sorted list - a leaderboard reordering itself by score.\n\nSample Input: Bob 30 Alice 25 Charlie 35\nExpected Output: [('Alice', 25), ('Bob', 30), ('Charlie', 35)]",
          criteria: ['sorted() is called with key=lambda t: t[1], so the sort is by age, not by name or the whole tuple', "For the sample input the program prints exactly \"[('Alice', 25), ('Bob', 30), ('Charlie', 35)]\""],
          hint: 'sorted(pairs, key=lambda t: t[1]).',
          solution: 'A sorted() call keyed by a lambda on the age field, producing the age-ascending tuple list.' },
      ] },
    { no: 13, week: 5, session: 1, title: 'Classes, Objects & __init__', video_url: 'https://www.youtube.com/watch?v=ZDa-Z5JzLYM',
      topic: "Real-life analogy: Think of an architect's blueprint that defines what every car has, versus each physical car rolling off the production line built from that one blueprint.\n\nA class bundles data (attributes) and behaviour (methods). __init__(self, ...) runs automatically when an object is created and sets up its starting state; self is the current object, passed first to every method.",
      problems: [
        { title: 'Student Profile Manager', points: 40, difficulty: 'Core',
          description: "Create a Student class with a name and a gpa attribute set in __init__, and a display() method that prints the profile line. Read a name and a GPA, build one Student, and call display() - a records system printing one student card.\n\nSample Input: Sara 3.85\nExpected Output: Student: Sara | GPA: 3.85",
          criteria: ['name and gpa are assigned in __init__ as self.name / self.gpa, and display() is a method that reads them via self', 'For input \"Sara 3.85\" calling display() prints exactly \"Student: Sara | GPA: 3.85\"'],
          hint: 'In __init__: self.name = name and self.gpa = gpa; then def display(self): print(f"Student: {self.name} | GPA: {self.gpa}").',
          solution: 'A Student class with __init__ setting name/gpa and a display() method printing the exact profile line.' },
      ] },
    { no: 14, week: 5, session: 2, title: 'Inheritance & Polymorphism', video_url: 'https://www.youtube.com/watch?v=RSl87lqOXDE',
      topic: "Real-life analogy: Think of a smart tablet that inherits every standard smartphone feature, then adds stylus support and a larger screen on top.\n\nclass Child(Parent) reuses the parent's code. super().__init__() runs the parent constructor so the child does not repeat that setup, and a child can override a method to change its behaviour while callers keep using the same name.",
      problems: [
        { title: 'Employee Hierarchy Manager', points: 40, difficulty: 'Core',
          description: "Create a base Employee class holding name and salary, and a derived Manager class that adds a department and calls super().__init__(). Read a name, salary and department, build one Manager, and print its summary line - an org-chart entry for a manager.\n\nSample Input: Daniel 85000 Engineering\nExpected Output: Manager: Daniel | Salary: $85000 | Dept: Engineering",
          criteria: ['Manager(Employee) subclasses Employee and calls super().__init__(name, salary) rather than re-assigning name/salary itself', 'For the sample input the program prints exactly \"Manager: Daniel | Salary: $85000 | Dept: Engineering\"'],
          hint: 'class Manager(Employee): def __init__(self, name, salary, dept): super().__init__(name, salary); then self.dept = dept.',
          solution: 'A Manager subclass extending Employee via super().__init__(), printing the exact manager summary line.' },
      ] },
    { no: 15, week: 5, session: 3, title: "File Handling with 'with' & Exceptions", video_url: 'https://www.youtube.com/watch?v=Uh2ebFW8OYM',
      topic: "Real-life analogy: Think of an automated vault door that unlocks as you step in and swings shut and locks itself the moment you leave - you never have to remember to close it.\n\nwith open(path) as f: guarantees the file is closed even if an error strikes mid-block. try / except / finally catches failures (a missing file, a bad value) so the program recovers instead of crashing.",
      problems: [
        { title: 'Safe Log File Writer & Reader', points: 60, difficulty: 'Boss',
          description: "Read one line of text. Inside a with block, write it to server.log; then reopen the file in read mode inside another with block and print its contents behind a label - the capstone finale, combining file writing, guaranteed closing and safe reading into one round trip, exactly like a vault that locks itself between visits.\n\nSample Input: STATUS_PORT_8080_ACTIVE\nExpected Output: Log File Content: STATUS_PORT_8080_ACTIVE",
          criteria: ['Both the write and the read happen inside with open(...) blocks (no manual f.close()), writing in \"w\" mode and reading in the default read mode', 'For input \"STATUS_PORT_8080_ACTIVE\" the program prints exactly \"Log File Content: STATUS_PORT_8080_ACTIVE\"'],
          hint: 'with open("server.log", "w") as f: f.write(text)  then  with open("server.log") as f: print("Log File Content:", f.read()).',
          solution: 'A write-then-read round trip through two with blocks, printing "Log File Content: STATUS_PORT_8080_ACTIVE" - capstone complete.' },
      ] },
  ],
},

{
  key: 'cs105-javascript', course_code: 'CS-105', free: true, friendly_grading: true, default_language: 'web',
  title: 'CS-105: Fundamentals of JavaScript',
  description: 'The language of the browser. Foundation-first, ending in a live interactive web app. Best taken after HTML & CSS. Four modules, ten topics, two tasks each, building a browser dashboard called PulseBoard with live DOM updates and persistence - certificate at the end.',
  outcome: 'Understand JavaScript variables, types, and equality, and use decisions and loops on real data; write functions and process arrays/objects with map, filter and object references; select, render and update the live DOM in response to events; persist app state in the browser with localStorage and JSON.',
  keywords: ['fundamentals of JavaScript', 'basics of JavaScript', 'free JavaScript course', 'learn JavaScript online', 'JavaScript for beginners'],
  key_concepts: ['console.log & where JS runs', 'Variables & types (let/const)', 'Operators, decisions & loops', 'Functions (declarations & arrows)', 'Arrays & their methods', 'Objects', 'DOM selection & rendering', 'Events', 'Live filtering & counters', 'localStorage & JSON'],
  pass_mark: 60, titleNames: ['Script Starter', 'DOM Builder'],
  levels: [
    { no: 1, week: 1, session: 1, title: 'console.log & Where JS Runs', video_url: null,
      topic: "JavaScript runs in the browser. console.log() prints to the developer console (F12), which is your main tool for testing logic before touching the page.\n\nconsole.log(\"PulseBoard ready\");\nconst item = { text: \"Ship v1\", done: false };\nconsole.table([item]);",
      problems: [
        { title: 'First Logs', points: 30, difficulty: 'Basic',
          description: "console.log() output only appears in the browser's developer console (F12), not on the visible page. Log a startup message and a few test values to the console.",
          criteria: ['console.log() is called with a startup message', 'The message and values appear in the browser console (F12), confirmed by opening it'],
          hint: "console.log('PulseBoard ready'); - open the console (F12) to see it.",
          solution: 'A startup message and test values logged, visible in the browser console.' },
        { title: 'Console Table', points: 40, difficulty: 'Core',
          description: "console.table() needs an array (or object) to render a grid - passing a single value won't produce a table. Log an array of objects using console.table to see it as a grid, and log types with typeof.",
          criteria: ["console.table() is called with an array of objects, rendering a grid", "typeof correctly reports each value's type (including the typeof null oddity if tested)"],
          hint: 'console.table(arrayOfObjects) needs an array, not a single value.',
          solution: 'An array of objects rendered as a console.table grid, plus typeof checks reported correctly.' },
      ] },
    { no: 2, week: 1, session: 2, title: 'Variables & Types (let / const)', video_url: null,
      topic: "let declares a variable you can reassign; const declares one you can't. JavaScript types include string, number, boolean, and object - you don't declare the type.\n\nconst item = { text: \"Ship v1\", done: false };\nlet count = 0;\ncount += 1;",
      problems: [
        { title: 'Store an Item', points: 30, difficulty: 'Basic',
          description: "Reassigning a const throws 'Assignment to constant variable' - that error is the guardrail working as intended. Hold a single board item's text and status in variables and log them.",
          criteria: ['const is used for values that never change, let for ones that do', 'Attempting to reassign a const is shown to throw, confirming the guardrail'],
          hint: 'Use const for fixed values, let for ones you plan to reassign.',
          solution: 'Item data stored with the right let/const choice, with the const-reassignment guardrail demonstrated.' },
        { title: 'The Equality Trap', points: 40, difficulty: 'Core',
          description: "== does type coercion (so 5 == '5' is true) while === checks type AND value (5 === '5' is false) - the rule is to ALWAYS use === to avoid surprise bugs. Compare values using == and === and document which cases differ and why.",
          criteria: ["At least one case (like 0 == '' or null == undefined) shows == and === disagreeing", 'A comment explains why === is the safer default choice'],
          hint: "Test cases that cross types, like 0 == '' or null == undefined.",
          solution: 'Side-by-side == vs === comparisons on cross-type cases, with a comment on why === avoids the surprises.' },
      ] },
    { no: 3, week: 1, session: 3, title: 'Operators, Decisions & Loops', video_url: null,
      topic: "Arithmetic and comparison operators feed if/else and switch; for and while loops repeat. These drive all the logic behind the interface.\n\nconst items = [{done:true},{done:false}];\nlet done = 0;\nfor (const i of items) if (i.done) done++;",
      problems: [
        { title: 'Status Logic', points: 30, difficulty: 'Basic',
          description: "In JS, non-boolean values are 'truthy'/'falsy' - an empty string is falsy, which can surprise beginners. Use if/else to decide how an item should display based on whether it's done.",
          criteria: ['if (item.done) { ... } else { ... } branches correctly', 'The right branch runs for both a done and a not-done item, with no truthiness surprises'],
          hint: 'if (item.done) { ... } else { ... } - watch for truthy/falsy edge cases.',
          solution: 'An if/else that correctly branches on item.done for both states.' },
        { title: 'Batch Processor', points: 40, difficulty: 'Core',
          description: "Dividing by a total of 0 gives NaN, so the calculation needs a guard for the empty-list case before dividing. Loop an array of items and build a summary: how many done, how many pending, and the completion percentage.",
          criteria: ['total === 0 is guarded before dividing, avoiding NaN', 'done, pending and percent are all counted/computed correctly for a non-empty list'],
          hint: 'Guard total === 0 before computing percent = (done / total) * 100.',
          solution: 'A summary loop with accurate counts and a NaN-safe percentage guarded for the empty case.' },
      ] },
    { no: 4, week: 2, session: 1, title: 'Functions (declarations & arrows)', video_url: null,
      topic: "Functions group reusable logic. Classic form: function add(){}. Arrow form: const add = () => {}. Arrows are shorter and common in modern JS, especially as callbacks.\n\nconst addItem = (items, text) => { items.push({ text, done: false }); };",
      problems: [
        { title: 'Core Functions', points: 30, difficulty: 'Basic',
          description: "Arrow/const functions aren't hoisted the way function declarations are - calling one before its declaration line says 'isn't defined'. Write addItem(), removeItem() and renderItems() as functions.",
          criteria: ['Each function does one job (add, remove, or render)', 'Every function is declared before it is called anywhere in the code'],
          hint: 'Arrow/const functions must be declared before use - they are not hoisted.',
          solution: 'Three named single-purpose functions, all declared before their first call.' },
        { title: 'Callbacks & Higher-Order', points: 40, difficulty: 'Core',
          description: "The callback itself must be invoked (action(i)) - passing action alone without calling it does nothing. Write a function that takes another function as an argument and applies it to every item (your own mini 'forEach').",
          criteria: ['The passed-in callback is actually invoked as action(i), not just referenced', 'Calling each(items, item => console.log(item)) runs the callback once per item'],
          hint: 'function each(items, action){ for (const i of items) action(i); }',
          solution: 'A mini forEach that correctly invokes the passed-in callback once per item.' },
      ] },
    { no: 5, week: 2, session: 2, title: 'Arrays & Their Methods', video_url: null,
      topic: "Arrays hold ordered lists. Beyond push/splice, the power methods map (transform), filter (select) and find (locate) let you process data cleanly without manual loops.\n\nlet items = [];\nitems.push({ id: 1, text: \"Ship v1\" });\nitems = items.filter(i => i.id !== 1);",
      problems: [
        { title: 'Item Store', points: 30, difficulty: 'Basic',
          description: "filter is the cleanest way to remove an item by rebuilding the array without it - items.filter(i => i.id !== targetId). Keep all items in an array; add with push and remove with splice or filter.",
          criteria: ['push adds new items to the end', 'filter (or a correctly-indexed splice) removes exactly the targeted item, not the wrong one'],
          hint: 'items = items.filter(i => i.id !== targetId); is the safest removal.',
          solution: 'push-based additions and a filter-based removal that always targets the correct item.' },
        { title: 'Transform Pipeline', points: 40, difficulty: 'Core',
          description: "filter, map and sort each return NEW arrays and chain together, leaving the original untouched - and order matters: filter first, then map, then sort. From the items array, produce a new array of just the titles of DONE items, sorted alphabetically - chaining filter, map and sort.",
          criteria: ['The chain runs in this order: filter (select done) then map (extract titles) then sort', 'The original items array is left untouched, and the result contains no undefined entries'],
          hint: 'items.filter(i => i.done).map(i => i.title).sort();',
          solution: 'A filter->map->sort chain producing a clean, sorted title list of only done items, original array untouched.' },
      ] },
    { no: 6, week: 2, session: 3, title: 'Objects', video_url: null,
      topic: "An object groups related data as key-value pairs: { id: 1, text: 'Buy milk', done: false }. This is how each board item is modeled, with named properties.\n\nconst item = { id: Date.now(), text: \"Buy milk\", done: false, tag: \"home\" };",
      problems: [
        { title: 'Model an Item', points: 30, difficulty: 'Basic',
          description: "If two items share an id, deleting one later removes both - each item needs a genuinely unique id. Represent each board item as an object with id, text, done and tag properties.",
          criteria: ['Every item object has all four properties: id, text, done, tag', "Each item's id is unique (e.g. via Date.now() or an incrementing counter)"],
          hint: 'Give each item a unique id, e.g. Date.now() or an incrementing counter.',
          solution: 'Item objects with all four properties and a genuinely unique id each.' },
        { title: 'Update by ID', points: 40, difficulty: 'Core',
          description: "Because objects are reference types, editing the object found by .find() edits it inside the array too - no need to reinsert it. Write a function that finds an item by its id and toggles its done status.",
          criteria: ["items.find(i => i.id === id) locates the item, using === (not ==) so mixed id types don't match wrongly", 'item.done = !item.done mutates the found reference directly, and the array reflects the change'],
          hint: 'const item = items.find(i => i.id === id); then item.done = !item.done;',
          solution: 'A find-and-toggle function that mutates the real array entry via its object reference.' },
      ] },
    { no: 7, week: 3, session: 1, title: 'DOM Selection & Rendering', video_url: null,
      topic: "The DOM is the browser's live model of your HTML. querySelector finds elements; you can create and inject elements to display your data on the actual page.\n\nconst container = document.querySelector('#list');\ncontainer.innerHTML = items.map(i => `<li>${i.text}</li>`).join('');",
      problems: [
        { title: 'Render the List', points: 30, difficulty: 'Basic',
          description: "If the script runs before the element exists, querySelector finds nothing - put the script at the end of body or use DOMContentLoaded. Take the items array and display it inside a container element on the page.",
          criteria: ["document.querySelector('#list') matches a real element on the page (correct # vs . usage)", 'The script runs after that element exists, and array contents actually appear on the page'],
          hint: 'Put the script at the end of body, or wait for DOMContentLoaded.',
          solution: 'A container correctly selected and populated with the array contents on the live page.' },
        { title: 'Re-render Cleanly', points: 40, difficulty: 'Core',
          description: "Appending without clearing first makes items duplicate on every action - render() must clear the container before rebuilding from the array, the one source of truth. Write a single render() function that redraws the whole list from the array, and call it after every change.",
          criteria: ["render() clears the container (innerHTML = '') before rebuilding from the array", 'Calling render() repeatedly after changes never produces duplicated items'],
          hint: "Clear innerHTML = '' at the start of render(), then rebuild from the array.",
          solution: 'A render() that always clears first and rebuilds from the array, called after every change with no duplication.' },
      ] },
    { no: 8, week: 3, session: 2, title: 'Events', video_url: null,
      topic: "Events are user actions - clicks, typing, submitting. addEventListener runs your function when they happen, connecting the interface to your logic.\n\naddBtn.addEventListener('click', () => {\n  items.push({ id: Date.now(), text: input.value, done: false });\n  render();\n});",
      problems: [
        { title: 'Add on Click', points: 30, difficulty: 'Basic',
          description: "If the page reloads on click, that's a form submit firing - call event.preventDefault() to stop it. Wire the Add button so clicking it reads the input and adds an item, then re-renders.",
          criteria: ["button.addEventListener('click', ...) is attached after the button exists in the DOM", 'Clicking Add reads .value, pushes a new item, and calls render() without reloading the page'],
          hint: 'Call event.preventDefault() if the click is inside a <form> to stop it reloading the page.',
          solution: 'A working Add button that reads the input, pushes a new item, and re-renders without a page reload.' },
        { title: 'Event Delegation for Delete', points: 40, difficulty: 'Core',
          description: "Listeners attached only to CURRENT buttons don't work on items added later - one listener on the container, checking event.target, works for every item including future ones. Handle delete-button clicks for ALL items (including ones added later) using a single listener on the container.",
          criteria: ['Exactly one listener is attached to the container, not one per delete button', 'event.target and its data-id correctly identify which item to delete, for items added before or after page load'],
          hint: 'One listener on the container; read event.target and its data-id to find which button was clicked.',
          solution: 'Event delegation on the container that correctly deletes any item, old or newly added.' },
      ] },
    { no: 9, week: 3, session: 3, title: 'Live Filtering & Counters', video_url: null,
      topic: "By reacting to input events, the interface updates instantly - filtering the list as the user types and recalculating counters on every change.\n\nsearchBox.addEventListener('input', () => {\n  const q = searchBox.value.toLowerCase();\n  render(items.filter(i => i.text.toLowerCase().includes(q)));\n});",
      problems: [
        { title: 'Search as You Type', points: 30, difficulty: 'Basic',
          description: "Listening for 'change' instead of 'input' only filters after the box loses focus - 'input' fires on every keystroke. Filter the visible items live as the user types in a search box.",
          criteria: ["The listener is on the 'input' event, not 'change'", 'The filter compares lowercase to lowercase (case-insensitive) and narrows the list on every keystroke'],
          hint: "Listen for the 'input' event, not 'change', and lowercase both sides of the comparison.",
          solution: "A live search filter reacting to the 'input' event, case-insensitive, narrowing results on every keystroke." },
        { title: 'Live Stats Dashboard', points: 40, difficulty: 'Core',
          description: "Tracking counters in separate variables lets them drift out of sync (increment on add, forget to decrement on delete) - they must be recomputed from the array every time. Show live counters (total, done, pending, % complete) that update on every add, delete, or toggle.",
          criteria: ['Every counter is recomputed from the current items array inside render() (or a function it calls), never tracked in a separate mutable variable', 'Counters stay accurate after an add, a delete, and a toggle in sequence'],
          hint: 'Recompute every stat from the array inside render() - never track counts separately.',
          solution: 'Stats recomputed from the array on every render, staying accurate through add/delete/toggle sequences.' },
      ] },
    { no: 10, week: 4, session: 1, title: 'localStorage & JSON', video_url: null,
      topic: "localStorage saves data in the browser that survives page refreshes and reopening. Since it only stores strings, you convert with JSON.stringify to save and JSON.parse to load.\n\nlocalStorage.setItem('items', JSON.stringify(items));\nconst saved = JSON.parse(localStorage.getItem('items')) || [];",
      problems: [
        { title: 'Never Lose Data', points: 30, difficulty: 'Basic',
          description: "Storing the array directly (without stringify) saves the useless text '[object Object]' - it must be converted to a JSON string first. Save the items array to localStorage on every change and load it on page start.",
          criteria: ['Saving uses JSON.stringify(items) before setItem, not the raw array', 'Loading uses JSON.parse on the retrieved string, and a refresh shows the data is still there'],
          hint: "localStorage.setItem('items', JSON.stringify(items)); - stringify before saving, parse after loading.",
          solution: 'A save/load pair using stringify and parse correctly, confirmed to survive a page refresh.' },
        { title: 'Robust Load & Reset', points: 60, difficulty: 'Boss',
          description: "On a true first visit getItem returns null, and JSON.parse(null) gives null - so it must default to an empty array with || [], and corrupted data needs its own try/catch. Handle the first-ever visit (nothing saved yet) and corrupted storage without crashing, and add a 'Clear All' button - the capstone finale.",
          criteria: ['JSON.parse(...) || [] defaults cleanly to an empty array on a genuine first visit (null case)', "Corrupted data is caught in a try/catch, and 'Clear All' calls localStorage.removeItem then re-renders"],
          hint: 'JSON.parse(saved) || [] handles the first-visit null case; wrap parse in try/catch for corrupted data.',
          solution: "A load routine safe on first visit and against corrupted data, plus a working 'Clear All' reset - PulseBoard complete." },
      ] },
  ],
},

{
  key: 'cs106-html-css', course_code: 'CS-106', free: true, friendly_grading: true, default_language: 'web',
  title: 'CS-106: Fundamentals of HTML & CSS',
  description: "One track, because CSS can't be taught without HTML underneath it. From a blank file to a responsive, styled multi-section website. Four modules, eight topics, two tasks each, building a complete responsive portfolio called DevFolio - structure first in HTML, then styled and made responsive with CSS - certificate at the end.",
  outcome: 'Structure a page correctly with semantic HTML, including lists, links, forms and accessible media; style it with CSS selectors, the box model, and correct specificity; build real layouts with Flexbox and CSS Grid; make it fully responsive with media queries and add professional polish with transitions.',
  keywords: ['fundamentals of HTML and CSS', 'basics of web design', 'free HTML CSS course', 'learn HTML CSS online', 'responsive web design for beginners'],
  key_concepts: ['HTML basics', 'Lists, links, forms & media', 'Selectors, colors & fonts', 'The box model', 'Flexbox', 'CSS Grid', 'Media queries & responsiveness', 'Transitions & finishing touches'],
  pass_mark: 60, titleNames: ['Markup Starter', 'Layout Architect'],
  levels: [
    { no: 1, week: 1, session: 1, title: 'HTML Basics', video_url: null,
      topic: "HTML describes the structure of a page using tags. Every page needs a basic skeleton (doctype, html, head, body). Tags like h1, p, a and img create headings, text, links and images.\n\n<!DOCTYPE html>\n<html>\n<head><title>DevFolio</title></head>\n<body>\n  <h1>My Portfolio</h1>\n  <p>Welcome to my work.</p>\n  <img src=\"profile.jpg\" alt=\"My profile photo\">\n</body>\n</html>",
      problems: [
        { title: 'Page Skeleton', points: 30, difficulty: 'Basic',
          description: "The most common miss is a missing alt on an image (needed for accessibility and SEO) or a broken src path. Create the portfolio page with a heading, an intro paragraph, and a profile photo.",
          criteria: ['<!DOCTYPE html>, then html > head (with title) > body is structured correctly', 'The <img> has both a src AND an alt attribute'],
          hint: 'Every <img> needs both src and alt.',
          solution: 'A correctly structured page skeleton with an h1, a p, and an img carrying both src and alt.' },
        { title: 'Semantic Structure', points: 40, difficulty: 'Core',
          description: "Semantic tags describe MEANING, not just grouping - header for the top, main for primary content, section for each block, footer for the bottom - which plain divs never communicate. Rebuild the page using semantic tags (header, main, section, footer) instead of generic divs everywhere.",
          criteria: ['<header>, <main>, <section> and <footer> replace what would otherwise be generic <div>s', 'Each semantic tag is used for the region that actually matches its meaning'],
          hint: 'header for the top, main for primary content, section per block, footer for the bottom.',
          solution: 'A page rebuilt with the correct semantic element for every region, no meaningless divs.' },
      ] },
    { no: 2, week: 1, session: 2, title: 'Lists, Links, Forms & Media', video_url: null,
      topic: "Lists (ul/ol) group items; nav holds navigation links; forms (input, textarea, button) collect user data. These build the interactive-looking parts of the page.\n\n<nav>\n  <a href=\"#about\">About</a>\n  <a href=\"#projects\">Projects</a>\n</nav>\n<form>\n  <label for=\"email\">Email</label>\n  <input id=\"email\" type=\"email\">\n</form>",
      problems: [
        { title: 'Nav & Contact Form', points: 30, difficulty: 'Basic',
          description: "Nav links only jump correctly when their href matches a section's id exactly, and each form field needs a matching label/input pair. Add a navigation bar with links to each section, and a contact form with name, email and message fields.",
          criteria: ["Each nav <a href='#id'> matches a real section id on the page", 'The form has matching <label>/input pairs for name, email (type="email") and message'],
          hint: "Nav links use <a href='#about'> pointing to a matching section id.",
          solution: 'A working in-page nav with matching hrefs, plus a labeled contact form (unstyled is fine).' },
        { title: 'Accessible Projects List', points: 40, difficulty: 'Core',
          description: "Meaningful alt text (not 'image1') and descriptive link text (not 'click here') are what make markup genuinely accessible to a screen reader. Mark up 3 projects with titles, descriptions, links and images, ensuring every image has alt text and links are descriptive.",
          criteria: ['Every project image has meaningful alt text describing the actual project, not a generic placeholder', 'Every project link uses descriptive text, not "click here"'],
          hint: 'alt should describe the specific project; link text should describe the destination, not say "click here".',
          solution: 'Three projects marked up semantically, each with meaningful alt text and descriptive link text.' },
      ] },
    { no: 3, week: 2, session: 1, title: 'Selectors, Colors & Fonts', video_url: null,
      topic: "CSS styles HTML. You select elements (by tag, .class, or #id) and set properties like color, background, and font-family. Link CSS with a <link> tag or a <style> block.\n\nbody {\n  background: #0D1117;\n  color: #E7ECF3;\n  font-family: sans-serif;\n}\n.accent { color: #38BDF8; }",
      problems: [
        { title: 'Apply the Brand', points: 30, difficulty: 'Basic',
          description: ". is a class selector and # is an id selector - confusing the two is why styles sometimes silently don't apply. Style the page with a dark background, accent color palette and a clean font.",
          criteria: ["<link rel='stylesheet' href='...'> correctly points to the CSS file (or a <style> block is used)", 'A base color/font is set on body, with accent colors applied via class or id selectors'],
          hint: '. selects a class, # selects an id - check you used the right one.',
          solution: 'The page adopts a consistent color/font look, correctly linked and selected.' },
        { title: 'Specificity Puzzle', points: 40, difficulty: 'Core',
          description: "Specificity order is id beats class beats tag - winning with the right selector is better practice than reaching for !important, which causes maintenance pain later. Create a case where two rules target the same element, and use specificity (not !important) to make the right one win.",
          criteria: ['Two rules genuinely target the same element with conflicting styles', 'The intended style wins purely through selector specificity, with no !important used, explained in a comment'],
          hint: 'An #id selector beats a .class selector, which beats a bare tag selector.',
          solution: 'A specificity-based override (id > class > tag) with no !important, explained in a comment.' },
      ] },
    { no: 4, week: 2, session: 2, title: 'The Box Model', video_url: null,
      topic: "Every element is a box with content, padding (space inside), border, and margin (space outside). Understanding this is essential - most layout confusion comes from the box model.\n\n.card {\n  padding: 16px;\n  border: 1px solid #333;\n  margin: 12px;\n  box-sizing: border-box;\n}",
      problems: [
        { title: 'Spacing & Cards', points: 30, difficulty: 'Basic',
          description: "Without box-sizing: border-box, adding padding makes a box WIDER than the width you set - a very common surprise. Give the project items padding, a border, and margin so they look like spaced cards.",
          criteria: ['padding, border and margin are all applied to make evenly spaced, bordered cards', 'box-sizing: border-box is set so padding does not inflate the box beyond its intended width'],
          hint: 'Set box-sizing: border-box so padding does not widen the box.',
          solution: 'Evenly spaced, bordered cards using padding/border/margin with border-box sizing.' },
        { title: 'Debug the Overflow', points: 40, difficulty: 'Core',
          description: "Fixed widths plus padding without border-box, or unexpectedly collapsing/adding margins, are the usual causes of content spilling out of its box. Given a layout that breaks (content spilling out), diagnose and fix it using the box model and box-sizing.",
          criteria: ['The actual cause is identified (fixed width + padding without border-box, or a margin issue), not just patched blindly', 'Applying box-sizing: border-box (often globally via *) and correcting the width math contains the layout cleanly'],
          hint: 'Use the browser element inspector to SEE the box, then apply * { box-sizing: border-box; } globally.',
          solution: 'The overflow cause correctly diagnosed and fixed with border-box sizing and corrected width math.' },
      ] },
    { no: 5, week: 3, session: 1, title: 'Flexbox', video_url: null,
      topic: "Flexbox arranges items in a row or column and controls their alignment and spacing. It's the go-to for navbars, centering, and one-dimensional layouts.\n\n.navbar {\n  display: flex;\n  justify-content: space-between;\n  align-items: center;\n}",
      problems: [
        { title: 'Navbar & Hero', points: 30, difficulty: 'Basic',
          description: "display: flex only affects the direct children of the element it's applied to - it must be on the actual container, not a distant ancestor. Use Flexbox to lay out the navbar (logo left, links right) and center the hero content.",
          criteria: ['display: flex is applied directly to the navbar container, with justify-content: space-between and align-items: center', 'The hero content is centered on both axes using flex'],
          hint: 'display: flex must be on the direct parent of the items you want to arrange.',
          solution: 'A flex navbar with logo/links spaced apart, and a flex-centered hero section.' },
        { title: 'Responsive Flex Wrap', points: 40, difficulty: 'Core',
          description: "The default for a flex container is nowrap, so items overflow off-screen unless flex-wrap: wrap explicitly lets them flow to a new line. Make a row of skill tags wrap neatly to the next line on small screens using flex-wrap and gap.",
          criteria: ['flex-wrap: wrap is explicitly set (not left at the nowrap default)', 'gap spaces the tags without margin hacks, and they wrap gracefully as the window narrows'],
          hint: 'flex-wrap: wrap is required - the default is nowrap.',
          solution: 'A tag row using flex-wrap: wrap and gap, wrapping cleanly as the viewport narrows.' },
      ] },
    { no: 6, week: 3, session: 2, title: 'CSS Grid', video_url: null,
      topic: "Grid handles two-dimensional layouts - rows AND columns at once. It's ideal for galleries and card grids like the projects section.\n\n.projects {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));\n  gap: 16px;\n}",
      problems: [
        { title: 'Projects Grid', points: 30, difficulty: 'Basic',
          description: "repeat(auto-fit, minmax(250px, 1fr)) is a powerful one-liner that makes columns fit automatically and wrap, without a fixed column count. Turn the projects into a responsive grid of cards using CSS Grid.",
          criteria: ['display: grid is set on the container, with grid-template-columns using repeat(auto-fit, minmax(...))', 'gap spaces the cards, and the grid adjusts column count automatically as width changes'],
          hint: 'grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); adjusts columns automatically.',
          solution: 'A responsive project grid using auto-fit/minmax that adjusts its column count to width.' },
        { title: 'Featured Layout', points: 40, difficulty: 'Core',
          description: "grid-column: span 2 makes an item occupy two tracks, but the grid needs at least 2 columns for that to mean anything, and the span should reset via a media query on mobile's single column. Make one project span two columns (a 'featured' project) while others stay single, using grid-column.",
          criteria: ['The featured card uses grid-column: span 2 while the grid has at least 2 columns available', 'A media query resets the span back to 1 column on mobile, so the layout does not break there'],
          hint: 'grid-column: span 2; on the featured card, reset via a mobile media query.',
          solution: 'A featured card spanning 2 columns on desktop, with its span reset to normal on mobile via a media query.' },
      ] },
    { no: 7, week: 4, session: 1, title: 'Media Queries & Responsiveness', video_url: null,
      topic: "Media queries apply different CSS at different screen widths, so the site looks right on phones, tablets and desktops. Mobile-first means designing for small screens, then adding rules for larger ones.\n\n<meta name=\"viewport\" content=\"width=device-width, initial-scale=1.0\">\n<style>\n@media (max-width: 600px) {\n  .navbar { flex-direction: column; }\n}\n</style>",
      problems: [
        { title: 'Mobile Reflow', points: 30, difficulty: 'Basic',
          description: "Without the viewport meta tag in the HTML head, mobile browsers won't scale correctly and the phone view looks like a shrunk desktop - a very common miss. Add a media query so the navbar and grid stack into a single column on narrow screens.",
          criteria: ['The viewport meta tag is present in <head>', '@media (max-width: 600px) stacks the navbar (flex-direction: column) and single-columns the grid'],
          hint: "Add <meta name='viewport' content='width=device-width, initial-scale=1.0'> to <head>.",
          solution: 'A working viewport meta tag plus a max-width media query that cleanly stacks the layout on mobile.' },
        { title: 'Three-Breakpoint Layout', points: 40, difficulty: 'Core',
          description: "A mobile-first approach starts with base (mobile) styles and adds complexity upward with min-width queries, at sensible breakpoints like 600px and 900px. Design distinct layouts for mobile, tablet and desktop (e.g. 1, 2, and 3 project columns) that transition smoothly.",
          criteria: ['Base styles are mobile (1 column), with min-width media queries adding 2 then 3 columns upward', 'Resizing the browser slowly shows each layout appear at the right width with no overlap or gaps'],
          hint: 'Mobile-first: base styles are mobile, then ADD complexity with min-width queries.',
          solution: 'Three clean, deliberately breakpointed layouts (1/2/3 columns) transitioning smoothly with no overlap.' },
      ] },
    { no: 8, week: 4, session: 2, title: 'Transitions & Finishing Touches', video_url: null,
      topic: "Transitions animate changes (like hover effects) smoothly. Shadows, hover states and small animations are the polish that makes a site feel professional.\n\n.card {\n  transition: transform 0.2s;\n}\n.card:hover {\n  transform: translateY(-4px);\n}",
      problems: [
        { title: 'Hover Polish', points: 30, difficulty: 'Basic',
          description: "Without the transition property, a hover change snaps instantly instead of animating smoothly - the transition line has to exist on the element itself. Add smooth hover effects to nav links and project cards (color change, subtle lift).",
          criteria: ['transition: ... is set on the element itself, not only inside :hover', 'The hover effect (color change or transform: translateY(-4px)) animates smoothly, not instantly'],
          hint: 'Put transition: all 0.2s on the base rule, then change the property inside :hover.',
          solution: 'Nav links and cards with a smooth, animated hover lift/color change.' },
        { title: 'Final Polish & Ship', points: 60, difficulty: 'Boss',
          description: "The goal of a ship-it pass is a portfolio that looks intentionally designed, not default - consistent spacing, shadows, hover states everywhere, and a keyframe entrance animation. Do a full review: consistent spacing, shadows, hover states everywhere, and a subtle entrance animation on the hero - the capstone finale.",
          criteria: ['A consistent spacing scale and box-shadows are applied throughout, with hover states on every interactive element', 'A @keyframes fadeIn (or similar) animates the hero on load, reviewed on both mobile and desktop'],
          hint: 'Define @keyframes fadeIn and apply it to the hero on page load.',
          solution: 'A fully polished DevFolio - consistent spacing/shadows, hover states everywhere, and a hero entrance animation - capstone complete.' },
      ] },
  ],
},

];
