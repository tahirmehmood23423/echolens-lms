'use strict';const{test}=require('node:test');const assert=require('node:assert/strict');const drafts=require('../public/js/draft-store');
const storage=()=>{const m=new Map();return{getItem:k=>m.get(k)||null,setItem:(k,v)=>m.set(k,v)}};
test('drafts isolate accounts, tasks and languages and preserve an explicit empty draft',()=>{const s=storage(),id=[1,'free','course',1,1,'python'];drafts.write(s,id,'print(1)',0);assert.equal(drafts.read(s,id).code,'print(1)');assert.equal(drafts.read(s,[2,...id.slice(1)]),null);assert.equal(drafts.read(s,[...id.slice(0,-1),'web']),null);assert.equal(drafts.read(s,[1,'free','course',1,2,'python']),null);drafts.write(s,id,'',1);assert.equal(drafts.read(s,id).code,'');});
test('an older delayed save cannot overwrite a newer draft',()=>{const s=storage(),id=[1,'paid',2,3,'python'];drafts.write(s,id,'first',0);drafts.write(s,id,'newer tab',1);assert.throws(()=>drafts.write(s,id,'old delayed save',1),/newer draft/);assert.equal(drafts.read(s,id).code,'newer tab');});
test('storage failures and unauthenticated persistence are reported',()=>{assert.throws(()=>drafts.write({getItem:()=>null,setItem:()=>{throw Error('Quota');}},[1,'x'],'code',0),/Quota/);assert.throws(()=>drafts.key([null,'x']),/Sign in/);});
